# Brief 539-2 — the house naming rule outranks the spec's getter names

## In plain words

The gate rejected three of the four new getters. The repo's own rule
says a static getter that returns a literal is a tunable constant and
must be named SCREAMING_SNAKE_CASE without the dollar prefix. Rename the
three regex getters and their call sites; the set getter stays as it is.

## End state (mechanically checkable)

Report newer than this stamp; `bash scripts/conventions-gate.sh` green
in your worktree; full `bun test` green.

## Items

1. Per the gate (static-getter-naming, three violations): rename
   `$identitySegment` -> `IDENTITY_SEGMENT`, `$semanticVersion` ->
   `SEMANTIC_VERSION`, `$windowsDeviceName` -> `WINDOWS_DEVICE_NAME` —
   plain `static get` (live, no $ cache; a regex literal per read is
   correct house form for a tunable constant and costs nothing on the
   admission path). Update the three `this.` call sites. Keep
   `$reservedVendors` exactly as is — it derives a Set, which is what
   the $ cache is FOR, and the gate accepted it.
2. Note in your report that this deviates from the external spec's
   naming deliberately: the repo's constants role table is the binding
   convention, and the spec's intent (module-variable zero, receiver-
   following reads, subclass-pinchable) is fully preserved.

## Bycatch expected

Unchanged.

## Invariants in scope

Unchanged from brief 539-1 (the ivue statics discipline / constants role
table is the rule being applied; no contract records implicated).
