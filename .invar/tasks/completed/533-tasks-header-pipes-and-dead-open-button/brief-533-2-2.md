# Brief 533-2 — scope extension: FULL visual parity with tasks:watch

## In plain words

The user says the tasks pane still does not look like tasks:watch. The
shared renderer landed, and the text matches, but "identical" means the
eyes agree: colors, tones, glyphs, motion, and phases — not just words.
Enumerate every visible difference, then close them all.

## End state (mechanically checkable)

The report contains a parity table: for each visible element (header,
scope row, gate row, task title row, detail row, phase glyphs, duration,
line deltas, attach text, hover states, motion), the pane's painted
cells (text + tone per segment) beside the CLI's ANSI output — every row
either IDENTICAL or fixed-then-identical. Plus one new smoke assertion
class: pane segment tones equal the shared projection's declared tones
(count-based, no wall clock).

## Known deltas to seed the table (conductor drives, 2026-08-06 ~10:0x)

1. Header paints literal pipes (round-1 item 1 — same fix).
2. Phase drift: pane showed "● building" while tasks:live showed
   "↑ exploring" for #532 at the same moment. Same projection must mean
   same phase at the same read — find whether the pane samples a
   different source or a stale cache.
3. Tone layer unverified: compare per-segment colors cell-by-cell (the
   PTY snapshot carries cells; COLORTERM=truecolor for asserts).

## The bar

Drive BOTH surfaces yourself, side by side, same fleet state, before and
after. The parity table is evidence, not prose — actual captured values.
DRIVE ADVERSARIALLY: hover, lens cycling, scroll, narrow width, ASCII
fallback, and motion all belong to "identical" (where the CLI genuinely
has no equivalent — hover, selection — the pane follows ui-design; name
those rows explicitly as pane-only, do not silently skip them).

## Invariants in scope

Unchanged from brief 533-1, plus the tasks-dashboard row-shape record.

## Bycatch expected

Unchanged.
