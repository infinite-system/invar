# Brief 435-5 — sweep the residual terminal terms

Your bycatch is accepted and the fix is confirmed under the same
scope: the notice replacement. Update the three adjacent phrases in
[tasks.invariants.md](../../../../src/modules/tasks/tasks.invariants.md):

- `File sources report displaced built-ins` — `Impossible if true`
  says `terminal list`.
- `Unsupported tasks fail visibly` — `Evidence` says `terminal cells`;
  `Impossible if true` says `running terminals`.

Reword each to the notice-pane truth. Checker green, commit
SKIP_GATE=1, append one line to your round-3 report file naming the
new commit. No new report file.

## Invariants in scope

The two records above; wording only, boundaries unchanged.

## Bycatch expected

Per the [AGENTS.md](../../../../AGENTS.md) taxonomy; `None observed`
is a valid section.
