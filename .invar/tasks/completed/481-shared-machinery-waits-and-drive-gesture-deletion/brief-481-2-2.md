# Brief #481 round 2 — the real assignment (round 1's brief was wrong)

## In plain words

Round 1's brief was the conductor's copy-paste mistake: it assigned work
that already landed, and you rightly changed nothing. THIS brief is the real
assignment from the task file: fix the waits inside the shared harness
machinery, and delete the vetoed gesture layer from Drive.ts.

## The work

1. **Shared-machinery wait sites** from the census
   ([the work list](../../completed/470-harness-wait-defect-census/census-470-harness-wait-defect-census.md),
   section A and the shell section): scripts/tui-harness.sh (the settle verb
   works since #470 — convert the worst sleep-as-sync sites to
   settle/condition waits; convert only where the condition is obvious,
   declare the rest remaining), scripts/harness/HarnessSmoke.ts,
   scripts/harness/PtyTestDriver.ts, scripts/harness/Drive.ts.
2. **Delete the --gesture/panel-role layer in Drive.ts** — the user-vetoed
   #466 direction, superseded by DriveSession. KEEP the generic
   status-excludes completion. Remove-the-capability discipline: a
   structural post-check proving no callers remain (grep the checkout + the
   smoke registry), every remaining Drive.ts consumer run after, and
   [scripts/harness/drive.md](../../../../scripts/harness/drive.md) updated.

## Invariants in scope

- [Harness waits observe conditions not frame ordinals](../../../../scripts/harness/harness.invariants.md)
- [Every wait names itself](../../../../scripts/harness/harness.invariants.md)
- [Drive clicks resolve from roles and text](../../../../scripts/harness/harness.invariants.md) — the deletion touches its territory; report whether the record needs a refines.
- [Coverage may fall but never silently](../../../../project.invariants.md) — DECLARE every wait/assertion decrease in [project.coverage-deltas.md](../../../../project.coverage-deltas.md), the vetoed-layer deletion included (name the veto as the reason).
- Any record this list MISSED is a finding about the conductor's map.

## Bycatch expected

Report per [AGENTS.md](../../../../AGENTS.md)'s taxonomy, even when None observed.

## PTY usability — still tracked

Report the `## PTY usability` section.

## Verification

Both arms where a control changes; every touched smoke green; the
structural no-callers post-check output in the report; `bun test` FULL;
`bunx tsc --noEmit`; conventions gate; checker `--all`/`--refs`.
NO merge-gate; SKIP_GATE=1.

## End state

Update the report in place with a round-2 section opening with
`## In plain words`.
