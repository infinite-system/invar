# Brief — #326 STAGE 1: vendor modularity — explore + design (no product code)

USER-DIRECTED, TWO-STAGE, SAME SESSION FOR BOTH STAGES. Read first:
[task-326-vendor-modularity-third-party-plugins.md](task-326-vendor-modularity-third-party-plugins.md)
— his verbatim words GOVERN; the record's seven stage-1 areas are the
work.

## Stage discipline (read carefully — this lane does not close at READY)

- STAGE 1 (now): explore the ACTUAL system and write
  a plan file named project-vendor-modularity-plan (a new .md in this task folder). NO product
  code. Record-only commit (SKIP_GATE with written verdict, quoted in
  the report).
- Your READY report ends stage 1. The lane STAYS OPEN: the plan goes
  to the user; his approval or amendments will be STEERED INTO THIS
  SESSION and you implement phase 1 with full stage-1 context. Do not
  exit, do not treat READY as done.

## Work discipline

- Census before design: #312's removable VuePlugin is the reference
  third-party-shaped citizen — quote its seams (plugin lifecycle,
  provider routing, Extensions surface, clean-removal polarity). The
  ivue Static-manifest conventions bind vendored modules too.
- Answer the user's naming questions (alphanumeric? '-' or '_'?
  digits?) with reasoned recommendations AND cited prior art (npm
  scopes, VSCode publisher IDs, crates, reverse-DNS). Reserve rules.
  Uniqueness mechanism phased convention->registry, tied to the
  Invar-internet north star in the repo-root briefing document (NORTH STAR section).
- Be honest at the trust boundary: name what is enforceable today vs
  honor-system; sandboxing out-of-scope phase 1 if that is the truth.
- Ranked open questions for the user close the plan.

## Invariants in scope

Read-only in stage 1: module/plugin records, ivue conventions,
Extensions records, #312's vue records. Stage 2 will modify — the plan
names which.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy; `## Bycatch` section
required even if `None observed`.

## End state (stage 1, mechanical)

READY report summarising the architecture + the user questions; plan
file committed in the task folder; zero product-code changes; session
stays alive awaiting the steered verdict.
