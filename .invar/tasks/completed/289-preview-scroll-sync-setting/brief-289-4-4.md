# Round 4 — #289: the activity-bar red is a harness ordinal, not your feature — fix the locator, then hook-commit

Diagnosis is complete, conductor-verified:

- Unmodified main `24dc2542`: `smoke-activitybar-harness` ALL-PASS solo.
- Your merged tree: the same smoke fails solo, deterministically (5th
  reproduction) — `Timed out waiting for the Structure dock-side setting
  is selected at its default`.
- Generator: the arm at
  [smoke-activitybar-harness.ts](../../../../scripts/harness/smoke-activitybar-harness.ts)
  (~line 451) presses `Down` ×14 and asserts `settingsSelected === 40 &&
  settingsSelectedLabel === 'Dock side'`. Your contributed
  `markdownPreviewScrollSync` setting adds a list row, shifting both the
  ordinal and the index. The harness navigates by POSITION where the
  contract is about a LABEL.

Fix at the harness seam, not your feature and not the wait budget:

1. Replace ordinal navigation with label-directed navigation: step Down
   until `settingsSelectedLabel === 'Dock side'` (bounded by the list
   length), and assert label + value — drop the absolute
   `settingsSelected === 40` (or derive it from the observed status,
   never hard-code).
2. Sweep this harness (and only it) for other position-coded
   `settingsSelected === N` / fixed Down-count patterns that a new
   contributed setting shifts — same fix shape for each you find.
3. Negative arm: a genuinely absent label must still fail loudly
   (bounded walk exhausts → timeout with the label named).
4. Then run the commit through the normal pre-commit hook (no
   SKIP_GATE). Panel-chrome passed its retry on your quiet run and is
   the known #214 intermittent — if it reds again quietly, quote and
   stop.

Update the READY report: the diagnosis (ordinal vs label), the sweep
result, both polarities, gate outcome.

## Invariants in scope

The activity-bar and settings records in
[ui.invariants.md](../../../../src/modules/ui/ui.invariants.md); the
harness-locator lesson mirrors the editorArea.title probe class — locate
by contract, not by coordinate.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy. The updated READY
report carries `## Bycatch` even if it reads `None observed`.
