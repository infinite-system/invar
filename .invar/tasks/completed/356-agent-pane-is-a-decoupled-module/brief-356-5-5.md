# Brief 356-5 — the skill popup lost its prefix filtering

## In plain words

One more agent surface broke: the skill popup no longer filters as
the user types. The smoke times out waiting for the /iv prefix to
filter the list to ivue. It fails deterministically, solo, on your
tip. Restore the filtering through the same generic seams.

## Reproduce EXACTLY

bun scripts/harness/smoke-agent-skill-popup-harness.ts
-> Timed out waiting for "the /iv prefix filters to ivue"

Suspects from your rounds: AgentSkillPopup is now constructed in
activateApplication with the contributed boundedListPopup/renderer;
its live-filter wiring (composer text -> popup filter) may have been
dropped in the generic pane input route, or the popup construction
happens before a dependency it needs, or disposal/re-registration
detached the text listener.

## End state (mechanically checkable)

The skill-popup smoke ALL-PASS on your tip; the round 2-4 smoke set
still green (keyboard bare, panel-split, activity bar, clipboard
frame boundary, both workspace smokes); report newer than this
filing stamp.

## Invariants in scope

Same running set (PaneContent citizen; focused-panel routing; one
popup host record if present). Answer record by record.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy even when None observed.

## Instrument feedback

EASY / CONFUSING / MISSING as usual.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 commits; the conductor
re-gates and lands.
