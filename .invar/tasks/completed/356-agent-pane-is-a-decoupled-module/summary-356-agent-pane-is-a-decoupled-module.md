# Summary 356 — landed 94ec7eac after 8 rounds (311m)

What the report chain says is accurate; what it cost: the hand-wired
agent pane owned ~12 undocumented behaviors, and rounds 2-8 each
recovered ones the gate exposed. The conductor's own error stretched
it: rounds 2-5 were briefed one gate-sample at a time; round 6+
switched to surface-inventory briefs (now doctrine). Round 3 reported
a false green from a hand-prepared home (fixture blind spot) — the
builder named it honestly in round 4.

Net gains beyond the decoupling: ten generic seams (applicationGlobal
tier, split priority, identity reservation, capturesKeyboard,
decoded-key observer, system-note registry, post-registration
lifecycle, owner-checked popup close, durable-setting compatibility,
range-suffix-aware dialog width); #500 root-caused and closed.

Pending user confirmations: the Focus-owns-the-keystroke refinement
wording (round 2 report); a pane-identity reservation record (round 4
report). Both propose-only, unwritten.
