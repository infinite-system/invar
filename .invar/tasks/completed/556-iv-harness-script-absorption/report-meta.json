{
  "touchedDomains": ["reports", "drift", "tasks", "graph", "server", "cli"],
  "invariants": {
    "upheld": [
      "Disk is the store and the graph is a projection",
      "The harness graph never imports from the app",
      "Only the files arbitrate process state",
      "A wrapped script keeps its logic in one place"
    ],
    "refined": ["A graph server is a disposable cache"],
    "discovered": []
  },
  "bycatchCount": 1,
  "steering": { "needed": false },
  "evidence": [
    "/tmp/gate-556.log",
    "iv-harness/src/modules/drift/HarnessDrift.test.ts"
  ]
}
