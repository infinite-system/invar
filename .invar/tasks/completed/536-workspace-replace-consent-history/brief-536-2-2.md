# Brief 536-2 — structural round: one applier, one state machine per concern, no copies

## In plain words

The behavior is right — the conductor drove the whole consent, apply,
undo cycle green. But the structure copied an editor method, folded two
state machines into one slot, and left the patch engine in the wrong
module. Close the items below; driven behavior must not change.

## End state (mechanically checkable)

Report newer than this stamp answering each item file:line; full
`bun test` green; the same consent/apply/undo drive from your report
re-run green after the refactor.

## Items (conductor's structural review; ranked)

1. Editor.ts:965 `applyTextEditsAsExternalTransaction` is a
   near-verbatim copy of :932 `applyTextEditsAsUndoStep` (same slice
   check, same descending sort, same replaceRange loop; the real deltas
   are all-or-nothing vs filter and the omitted captureBefore). Extract
   one protected `applyVerifiedEdits(edits)` primitive; history capture
   becomes the caller's step. The two paths must be unable to diverge.
2. The multi-patch application engine (ordering by verified offset,
   overlap detection, chunk splice, offset bookkeeping) lives in
   WorkspaceSearchWorkspace (:466 region) — patch-layer domain logic in
   search/. Move it to workspace/ beside TextPatch (with the
   byte-offset->line/column conversion at :535-577); search calls the
   seam. The next provider (rename, code actions) must not write it
   again. Also: TextPatch.createRecorded (TextPatch.ts:62) is a
   validation-free twin of create — close or justify the escape hatch.
3. WorkspaceSearchWorkspace.ts:922: twelve replacement states folded
   into the search flowState — two machines, one slot ("applying"
   erases search state; "ready" after cancel is ambiguous). Split a
   separate replacement/bulk state beside the search state, per your
   own sibling precedent (FindBar.ts:204 bulkFlowState). Then unwind
   the direction ternaries where the split makes them natural
   (showPreparedDialog's four).
4. One patch geometry: WorkspaceSearchBackend.ts:77/:97 harvest context
   with bare literal 64 — read TextPatch.CONTEXT_BYTE_LENGTH.
5. Copies: bytesEqual now x3 (TextArena.ts:134 pre-existing, Files.ts:244,
   WorkspaceSearchWorkspace.ts:720) — one home; classify/applyEntries
   share a ~20-line prologue — extract; the line-break scan appears in
   Backend.sourceContext:48 and Workspace.positionAtByteOffset:554 —
   one helper; the openExclusiveOverlay('quitConfirmation', ...) block
   is now FIVE copies across PaneContent and Bootstrap — extract one
   confirm() helper on the application surface (do NOT rename the
   overlay key in this task — #526 owns the rename).
6. Guard coverage: the pane Undo button calls search.prepareUndo()
   directly (PaneContent.ts:377) while editor undo goes through
   coordinator.request — route both through the coordinator so the
   re-entrancy guard covers both.
7. ivue nits: itemNoun/fileNoun/locationText arrows inside a method
   body (PaneContent.ts:631) become prototype methods; restore the
   state->derived->methods order broken at Workspace.ts:161-726; one
   shared TextEncoder; unfold `ordered`/`first`/`second` names.

## Invariants in scope

Unchanged from brief 536-1.

## Bycatch expected

Unchanged.
