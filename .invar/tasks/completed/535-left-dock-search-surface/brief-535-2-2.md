# Brief 535-2 — structural round: one generator per behavior

## In plain words

The panel works — the conductor drove icon, chord, fields, streaming
results, and click-to-line, all green. But the structure has copies
where generators should be, and the option buttons landed on accidental
rows. Close the five findings below; behavior must not change except
where item 4 names it.

## End state (mechanically checkable)

A report newer than this brief's stamp answering each item with the
file:line of the fix; full `bun test` green; the panel re-driven after
the refactor (same drives as your report, unchanged results).

## Items (from the conductor's structural review)

1. WrapText: WorkspaceSearchPaneRenderer.ts:373-388 (`wrap`) and
   WorkspaceSearchResultTree.ts:240-257 (`limitNoticeLines`) hand-roll
   the greedy word-wrap that already exists as `WrapText.Class.wrap`
   (src/modules/ui/WrapText.ts:79, already imported by your
   PaneContent). Route both through it; delete the copies.
2. Render context: WorkspaceSearchPaneContent.ts:201-226 and :648-666
   build the same 14-field context twice, and the copies already
   disagree (focused, hoveredField, selectionRanges) — hit-test text
   and drawn text can drift. Collapse to one `renderContext(...)` with
   a thin interaction caller, per the sibling
   TasksDashboardPaneContent.ts:166/:218 form.
3. One geometry source: `resultStartRow` is a plain field `6` in
   PaneContent.ts:93 AND a bare literal 6 in PaneRenderer.ts:160. Make
   it one static the renderer reads (`static get RESULT_START_ROW()` on
   the geometry owner); both consumers read that.
4. Options row (the one design change): the fieldDefinitions table
   (PaneRenderer.ts:23-53) allows one button per field row, so `.*`
   sits on the Replace row, `ab` on Files-to-include, and the fourth
   option needed the forcedWidth escape hatch (:134-142). Lift the four
   options into their OWN options row (per the ui-design buttons
   chapter); field rows carry fields, the options row carries options.
   Re-drive toggles after.
5. Keyboard path: `toggleIgnoreFiles` and `dismissMatch` are clickable
   but have no keybinding or command — a visible option needs a
   keyboard path. Add commands/chords consistent with your existing
   Search chord family, and state them in the report.

## Invariants in scope

Unchanged from brief 535-1, plus the find-bar "one geometry source"
clause your item-3 split implicates.

## Bycatch expected

Unchanged.
