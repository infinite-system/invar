# Brief 518-2 — the merge with main inverted your row counts, plus two drive timeouts

## In plain words

Main changed how the tasks pane tests see the fleet root while you worked.
Your merged tree now paints one more row than your tests expect. Fix the
expectations against the pinned environment, and fix two drives that time
out.

## End state (mechanically checkable)

A report newer than this brief's filing stamp, and in your worktree these
pass: `bun test src/modules/tasks-dashboard/`,
`bun scripts/harness/smoke-diagnostics-harness.ts`,
`bun scripts/harness/smoke-panel-chrome-harness.ts`.

## Items

1. [TasksDashboardPlugin.test.ts](../../../../src/modules/tasks-dashboard/TasksDashboardPlugin.test.ts):
   main commit 33b4b57a pins INVAR_FLEET_REPOSITORY_ROOT for the whole
   file (read its comment block — the scope row was environment-dependent
   before; green in builder worktrees, red on the main checkout). Under
   the pinned override the workspace is NOT its own fleet root, so the
   scope row ALWAYS renders. Your merged test expects 2 rows at line 243
   (receives 3) and detail-row attachment length 1 at line 356 (receives
   0). Re-derive your expectations under the pinned environment — do not
   remove the pin, and do not make counts depend on the runner's env.
2. smoke-diagnostics-harness: awaitGridCondition timeout (failure log in
   /tmp/merge-gate-failures.255636bb2d9f2721.1816399/). Your shared
   renderer or 60fps heartbeat may repaint rows the harness waits through.
   Drive it and see.
3. smoke-panel-chrome (contention tier, retried and failed): timeout at
   awaitStatusWithoutFrame. This file changed in #514 which landed under
   you — check your merge kept its round-2/3 semantics (status control is
   no-create; Ctrl+J selects or creates). Drive the exact failing step.

## The bar

Reproduce by driving first. Fix behavior or expectations to match the
DESIGN, never the runner's environment. No timeout widening. If you
believe a smoke expectation is wrong, argue it in the report and leave it
red for the conductor.

## Invariants in scope

Same records as your round-1 brief (tasks-dashboard contract: one stable
row shape per lens), plus the pinned-env rule from main's test comment.

## Bycatch expected

Report per the taxonomy in [AGENTS.md](../../../../AGENTS.md), even when
None observed.
