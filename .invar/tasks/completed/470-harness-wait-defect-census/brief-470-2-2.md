# Brief #470 round 2 — the gate found consumers your reset breaks

## In plain words

Your fixes are right in aim, but the quiescence reset changes how many
frames other checks observe. Three blocking checks now fail on the combined
tree. Fix the reset so real waits work AND frame accounting stays exact.

## The failures (combined tree = your branch + main, commit 9954f9db)

1. [behavioral-contracts.sh](../../../../scripts/behavioral-contracts.sh) — BLOCKING, two failures:
   - `scale-invariance document-line reads-per-frame ratio 100000/2000=1.003077,
     expected exact 1 (counts 326/5 vs 260/4)` — the frame COUNTS differ
     (5 vs 4). Your reset likely causes an extra settled frame or an extra
     status flush that the contract observes.
   - `glide-cap 100 ms drive failed`.
2. [the settings-applied smoke](../../../../scripts/harness/smoke-settings-applied-harness.ts) — BLOCKING:
   `FAIL scrollAccelGain 120 moves farther than 5 (1 to 1)` — both gains
   moved exactly 1 line, so a wait in that smoke now samples mid-gesture or
   the gesture is cut short.
3. `contention: panel-chrome` failed again on this run — check whether your
   HarnessSmoke count-wait interacts; the log is at
   /tmp/merge-gate-failures.d0c9bd200014754a.2444345/ (all four logs are
   there; read them first).

## The likely shape

A reset that fires on EVERY requestRender may publish extra status writes or
change frame parity that exact-count contracts observe. Consider: reset only
on the first request after a settled frame (you may already do this — then
find the second write), and make sure the reset itself does NOT flush a
status file write (a flush per request would double the observed writes).
The contracts requiring EXACT frame counts are the specification here: your
change must be invisible to them.

## Rules unchanged

Both arms again after the fix; run the three failing checks yourself (they
are conductor-tier but you may run them as FOCUSED checks: behavioral-contracts.sh,
the settings-applied smoke, the panel-chrome smoke) — but still NO
scripts/merge-gate.sh. Commit with SKIP_GATE=1. Update the report in place
with a round-2 section.

## Invariants in scope

Same records as [round 1](brief-470-1-harness-wait-defect-census.md#invariants-in-scope), plus the two your round-1 report added:
- Rendering is one coarse frame effect ([app contract](../../../../src/modules/app/app.invariants.md))
- Shared seam changes verify every consumer ([harness contract](../../../../scripts/harness/harness.invariants.md)) — the three failures above ARE that record firing.
Also note: the panel-chrome contention failure is the drag-span wait at 120 columns ("a drag begun on the last cell of the drag span still resizes the panel") — the census's known class-1 precondition at [smoke-panel-chrome-harness.ts](../../../../scripts/harness/smoke-panel-chrome-harness.ts) lines 712-717; if your count-wait change did not touch it, say so and leave it (it is a tracked #470 migration item), do not fix it in this round.

## Bycatch expected

Report per [AGENTS.md](../../../../AGENTS.md)'s taxonomy in the round-2 report section, even when it reads None observed.
