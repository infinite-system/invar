# Brief 514-3 — baseline skew plus two dialog-adjacent reds

## In plain words

The gate is red for three reasons. One is arithmetic: your vocabulary
baseline of 30 predates the merge with main. Two are real drives that time
out around dialogs and the status control. Fix the number honestly and fix
the two behaviors.

## End state (mechanically checkable)

A report newer than this brief's filing stamp, and in your worktree these
pass: `bash scripts/conventions-gate.sh` (vocabulary step),
`bun scripts/harness/smoke-overlay-dialog-harness.ts`,
`bun scripts/harness/smoke-panel-chrome-harness.ts`.

## Items

1. Vocabulary ratchet: you tightened the baseline to 30 when your
   pre-merge tree held 30 sites. Main moved to 33 sites; your merged tree
   holds 32 (you still remove one net site — the StatusBar.ts entry). Set
   VOCabulary_BASELINE=32 in scripts/conventions-gate.sh with a one-line
   comment naming the merge as the reason. Do NOT set it back to 33.
2. smoke-overlay-dialog: timeout at "the first post-dismissal press
   reaches the underlying status action" (failure log
   /tmp/merge-gate-failures.bd0013ddbc854489.1624625/). Your status
   control now projects through the generic renderable — check that a
   press immediately after dialog dismissal still lands on the status
   action on the FIRST press. Drive it before reading code.
3. smoke-panel-chrome (contention tier, retried and still failed):
   timeout at "10-line add header press cancels cleanly" — your own
   edited harness. Drive the 10-line fixture's add-header cancel path.

## The bar

Same as round 2: reproduce by driving first, fix behavior, never widen a
timeout or weaken an assertion. If a smoke's expectation is wrong under
the new design, argue it in the report and leave it red for the conductor.

## Invariants in scope

Same records as brief 514-2.

## Bycatch expected

Report per the bycatch taxonomy in [AGENTS.md](../../../../AGENTS.md),
even when None observed.
