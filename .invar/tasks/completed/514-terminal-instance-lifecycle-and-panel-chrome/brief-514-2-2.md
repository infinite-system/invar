# Brief 514-2 — the gate is red on neighboring terminal flows and one isolation leak

## In plain words

Your changes landed cleanly in your own drives, but the merge gate shows
four neighboring flows now time out waiting for a terminal to be focused,
and a new workspace inherits the contents-list pin from the old one. Fix
the behavior, not the smokes. Also two of your coverage declarations have
stale wait counts.

## End state (mechanically checkable)

A report file newer than this brief's filing stamp exists in the task
folder, and these commands each pass in your worktree:
`bun scripts/harness/smoke-workspace-tabs-harness.ts`,
`smoke-workspace-layout-isolation-harness.ts`, `smoke-database-harness.ts`,
`smoke-paste-harness.ts`, `smoke-scrollbars-harness.ts` — plus
`bun scripts/coverage/check-coverage-ratchet.ts` (or the gate's coverage
step) green.

## Evidence (gate log /tmp/gate-514.log, failure dir /tmp/merge-gate-failures.bd0013ddbc854489.1526167)

1. smoke-workspace-tabs: timeout at "the first workspace owns its
   interactive terminal".
2. smoke-database: timeout at "Terminal receives focus before opening
   Database: Reconnect".
3. smoke-paste: timeout at "the focused terminal keeps the dropped paths
   out of the editor".
4. smoke-scrollbars (contention tier): awaitGridCondition timeout.
5. smoke-workspace-layout-isolation: "a new workspace opens with its
   contents list unpinned (expanded=false, painted width 20, workspace A
   holds 27)" — the new workspace inherits workspace A's pinned/expanded
   contents list.
6. Coverage declarations stale: [project.coverage-deltas.md](../../../../project.coverage-deltas.md):16
   (smoke-agent-harness: declared waits 8 -> 8, actual 8 -> 6) and :41
   (smoke-panel-split-harness: declared waits 33 -> 33, actual 33 -> 40).
   Re-declare with the true counts and one-line reasons.

## Ranked candidates (hypotheses, not a diagnosis)

- Your "+ Terminal is not selected right away" and "one toggle button, no
  auto-instance" changes altered when a newly opened terminal takes focus
  or publishes its status. The neighboring flows wait on those
  publications. Check what the old path published and when.
- The contents-list expanded/pinned state you touched for the overlay
  hover work may now persist globally instead of per workspace.

## The bar

Reproduce each failure by DRIVING it first (the smokes name the exact
flows). Fix the behavior. Do not widen a timeout, do not edit a
neighboring smoke's assertion to pass — if you believe a smoke's
expectation is now WRONG under the new design, say so in the report with
the reasoning and leave the smoke red for the conductor's call.

## Invariants in scope

Same records as brief 514-1, plus re-verify your refined "Panel controls
share paint and hit geometry" against the isolation failure.

## Bycatch expected

Report per [AGENTS.md](../../../../AGENTS.md)'s bycatch taxonomy even when None observed.
