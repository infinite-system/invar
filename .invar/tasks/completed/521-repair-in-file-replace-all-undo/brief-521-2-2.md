# Brief 521-2 — the gate is red on the quit dialog neighbor and one drag contention

## In plain words

The merge gate shows the quit dialog's own drive timing out. You extended
the shared dialog model that quit uses, so the closest neighbor of your
change is the first suspect. One panel drag check also timed out under
load.

## End state (mechanically checkable)

A report newer than this brief's filing stamp, and in your worktree these
pass: `bun scripts/harness/smoke-quit-confirmation-harness.ts` and
`bun scripts/harness/smoke-panel-chrome-harness.ts`.

## Items

1. smoke-quit-confirmation: awaitGridCondition timeout (failure log
   /tmp/merge-gate-failures.ca4dd900a63d01c3.1995990/). Your consent
   controller now routes quit, Replace All, undo, and redo through the
   shared model in Bootstrap. Drive the quit path first: unsaved file,
   Ctrl+Q, the dialog, both buttons, Escape. Check flow-state ownership —
   if FindBar's consent states and quit's dialog share a slot or a flag,
   a stale state can block the quit dialog from opening or closing.
2. smoke-panel-chrome (contention tier, retried and still failed):
   "120-column a drag begun on the last cell of the drag span still
   resizes the panel". This smoke passed on your branch pre-merge only if
   you ran it; it has been load-sensitive this week. Run it solo and
   under two concurrent full-smoke processes. If it never fails for you,
   say so with the run counts and leave the verdict to the conductor.

## The bar

Drive first, fix behavior, no timeout widening, no assertion weakening.

## Invariants in scope

"Input overlays share one modal slot"
([ui.invariants.md](../../../../src/modules/ui/ui.invariants.md)) — your
report claims upheld; item 1 is exactly where that claim gets re-tested.

## Bycatch expected

Report per the taxonomy in [AGENTS.md](../../../../AGENTS.md), even when
None observed.
