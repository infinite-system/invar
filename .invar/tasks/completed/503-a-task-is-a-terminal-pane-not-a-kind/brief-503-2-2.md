# Brief 503-2 — the first workspace no longer owns its interactive terminal

## In plain words

One smoke reds deterministically on your tip: workspace-tabs times
out at "the first workspace owns its interactive terminal". Your kind
change likely altered how the built-in interactive terminal (the
no-file built-in task) is identified or claimed per workspace. Drive
it, name the mechanism, fix through the same seams.

## Reproduce EXACTLY

bun scripts/harness/smoke-workspace-tabs-harness.ts
-> Timed out: "the first workspace owns its interactive terminal"
(solo, twice, on your merged tip.)

## End state

That smoke ALL-PASS bare; your #503 smoke arms still green; report
newer than this filing stamp naming the mechanism.

## Invariants in scope

The 503 set (tasks + ui records). Answer for the repair.

## Bycatch expected / Instrument feedback

As always.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 commits; the conductor
re-gates and lands.
