# Brief 354-3 — correct the move-line coverage declaration

## In plain words

Your race fix changed the move-line smoke's assertion and wait counts,
but the coverage declaration you wrote does not match. The ratchet gives
the exact right numbers. Fix the one line.

## End state (mechanically checkable)

`bash scripts/conventions-gate.sh` (or the gate's coverage step) green;
[project.coverage-deltas.md](../../../../project.coverage-deltas.md) line for smoke-move-line-harness.ts matches
the actual counts.

## The fix

[project.coverage-deltas.md](../../../../project.coverage-deltas.md):175 declares `assertions 7 -> 6, waits 8 -> 8`.
The ratchet's ACTUAL counts are `assertions 6 -> 6, waits 8 -> 7`. Write
those, with a one-line reason: the openFileThroughQuickOpen helper
replaced a pre-satisfied Enter race with the helper's ranked-match wait
(one fewer local wait; assertions unchanged at 6). The old "7 -> 6"
from-count was itself wrong (the base had 6, not 7).

## The bar

Only the declaration line. No smoke change. Re-run the coverage step to
confirm green.

## Note (conductor)

The scrollbars contention red in the same gate is the known load flake
(#547 family) — not yours; a solo re-gate clears it.

## Invariants in scope

Unchanged.

## Bycatch expected

Unchanged.
