# Brief 522-2 — the popup smoke is red on your branch; your input encoding is the suspect

## In plain words

The gate shows the bounded-list-popup drive failing on your branch,
retried and still red. Your batch changed how the harness encodes key
input. The popup smoke consumes that encoding. Prove whether your
change broke it or the load did, then fix what is yours.

## End state (mechanically checkable)

Report newer than this stamp; `bun scripts/harness/smoke-bounded-list-popup-harness.ts`
green in your worktree solo AND under 2x contention; if the red proves
PRE-EXISTING or load-only, the merge-base run counts and logs that
prove it.

## Evidence

/tmp/merge-gate-failures.868b1bb114d9a50b.1638619/smoke-bounded-list-popup-*.log
— retried and still failed, during a 3-gate contention window. Your
diff touches HarnessInput (modified-key chords) and DriveSession
(paste, scoped click) — the popup smoke drives through exactly those
byte paths.

## The bar

Merge-base discipline: run the smoke at the merge base under the same
conditions before blaming load (bycatch on the changed tree cannot
distinguish revealed from caused). No timeout widening. If your chord
encoding changed any existing byte form (not just added new ones), that
is the first place to look.

## Invariants in scope

The founding harness invariant (input travels as real PTY bytes a
terminal could produce) — your chord bytes must be forms a real
terminal emits.

## Bycatch expected

Unchanged.
