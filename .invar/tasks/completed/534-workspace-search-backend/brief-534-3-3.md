# Brief 534-3 — one residual test still assumes ripgrep exists

## In plain words

One workspace test still reaches the real ripgrep resolver, so on this
machine it finds nothing and fails. Give it the same injected resolver
your backend tests use.

## End state

Report newer than this stamp; `bun test src/modules/workspace/Workspace.test.ts`
green on this rg-less machine; then re-verify the FULL `bun test` run.

## Evidence

Gate log /tmp/merge-gate-failures.2c4ce89b2463d45f.3181638/: Workspace.test.ts:53
"each workspace owns independent workspace-search state" expects 1
result, receives 0 — the search ran against the real absent rg.

## The bar

Fix the test's seam injection, not the assertion. Sweep the rest of the
suite for any other site that reaches the real resolver (grep for the
resolver seam's default path) so this is the LAST residual, not the
next one.

## Invariants in scope

Unchanged from brief 534-2.

## Bycatch expected

Unchanged.
