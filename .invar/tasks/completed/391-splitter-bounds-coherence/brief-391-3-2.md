# Brief 391-2 — CORRECTION: the trigger is LAYOUT SWITCHING, not window resize

User correction (2026-07-31, verbatim): "the layout problem is when
you switch layouts, not on resize"

Re-aim step 1: reproduce by DRIVING the LAYOUT SWITCH path — cycle
through the layout modes/presets (panel positions, split arrangements,
whatever the layout switcher offers) with left/right panes open, and
observe the panes come back stuck at a smaller size after a switch
(and/or after switching away and back). Window resize is NOT the
trigger; do not spend further drives on it beyond one control run.

The seam suspects stand: unclamped size writes and min/max
incoherence can fire on a layout switch exactly as on a resize —
plus a third candidate now: the layout switch REBUILDING splitters
and seeding them from a default or stale persisted size instead of
the live pane size. Name which candidate the driven numbers convict.

Everything else in brief-391-1 holds: fix at the seam, remembered
size restored, count-based smoke on the SWITCH path (pane width after
switch-away-and-back == width before), stressed-record re-verify.

## Invariants in scope
Unchanged from brief-391-1: "A reported size never leaves its
configured bounds" ([layout.invariants.md](../../../../src/modules/layout/layout.invariants.md))
plus sibling layout records the seam touches — answer each; refute
any missed.

## Bycatch expected
Report per [AGENTS.md](../../../../AGENTS.md) taxonomy; include a ## Bycatch section
even when it reads: None observed.
