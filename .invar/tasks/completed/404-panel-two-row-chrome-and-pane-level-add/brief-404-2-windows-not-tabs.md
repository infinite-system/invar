# Brief #404 round 2 — clarification: windows, not nested tabs

The user clarified, verbatim: "In the same terminal i meant more
terminal/agent/invar agent windows, not tabs, tabs are outer control to
make more terminal containers, database containers, etc"

So: the outer tab row creates CONTAINERS (terminal container, database
container). Inside a container, the lower-level + adds WINDOWS (split
panes) of Terminal / AI Agent (Claude) / Invar Agent. No tab bar inside
a container, ever. If any part of round 1's brief read otherwise, this
wins. End state unchanged otherwise; the report acknowledges this
clarification explicitly.

## Invariants in scope

Unchanged from round 1.

## Bycatch expected

Unchanged from round 1.
