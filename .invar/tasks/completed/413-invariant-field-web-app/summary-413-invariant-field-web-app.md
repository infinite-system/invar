# Summary #413 — the Invariant Field

Landed df9419cc, 77 minutes dispatch to landing, one builder (sol
HIGH, user-assigned), zero overlap with main, gate ALL-PASS 66/66 no
retry.

What happened:

- Dispatched with the theory corpus (3 Field versions, 3 papers), then
  primed in flight by three round briefs: the canonical checker +
  annotation statistics, the full IBR framework (with the axiom-map
  end state), and the ivue + TypeScript conventions we forgot at
  dispatch. The builder absorbed all of it and the READY answered
  every order explicitly.
- The rank came out as 11 axiom-mapped components with an asymptotic
  radius; the calibration arms all held, including planted rot moving
  a copied record outward 0.077 on a scratch directory, and inverted
  positive controls proving both the rank test and the composition
  test can go red.
- The lattice projection had a real defect mid-build (zero record
  memberships while highlighting worked) — the builder found and fixed
  it; curvature input is now test-locked.
- Bycatch: two rotted evidence citations across the contract layer ->
  #414 (fixing them will visibly move two dots inward — the
  instrument demonstrating itself).
- Conductor-side: this lane burned in the steer landed-verification
  machinery — two fragment defects (trailing period, mid-span em-dash)
  found and fixed the same hour; both steers ultimately confirmed
  LANDED at the rollout; one false STEER_LOST corrected.

Run it (verified by driving): bun tools/invariant-field/server.ts from
the repo root -> http://localhost:4313/. First run rebuilds the
gitignored snapshot store (~3 min, 308 snapshots); later runs are
instant unless HEAD moved. (An earlier draft of this summary said
'bun run serve' — an instruction written without running it.)
