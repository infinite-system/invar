# 190 — pool membership must be earned and declared

State: ACTIVE — premise corrected in place
Created: 2026-07-28
Engine: claude
Environment: linux
Model: fable-5
Effort: medium
Priority: verification-integrity
Assignment note: Inverts a default across every smoke; the grandfathering call is the risk.

## Outline

### The proposal: invert the default

**A smoke is SERIAL unless it declares pool membership WITH ITS EVIDENCE** — a date plus N/N pool runs.
Today the default runs the other way, so a smoke enters the concurrent pool by nobody's decision.

- **Grandfather the currently-pooled set** with a dated note saying the evidence is historical, rather
  than backfilling proof nobody actually took. An honest label beats a manufactured record.
- **Positive control in both directions**: an undeclared throwaway smoke must run serially, and adding
  the declaration must move it into the pool. A registration mechanism that cannot be observed to work
  is not a mechanism.

### The correction this task carries — my premise outran the evidence

The in-pool-retry section of doctrine was written using `reserved-chord` as its **confirmed instance**,
and this task was filed saying its unproven pool registration "became one of two things blocking main."

**Both statements outran the evidence.**

- **The retry PRINCIPLE survives on its own logic**: an in-pool retry reproduces the very condition it
  was meant to rule out. That does not need an instance.
- **The INSTANCE does not survive.** `reserved-chord` was two gate failures followed by **eight
  subsequent passes** — *an unreproduced red*, not a demonstrated pool defect.

Corrected **in place**, in the skill and here, rather than quietly softened.

**Why it mattered enough to correct rather than let stand:** filing it as "pool-caused" would have sent
a builder to CONFIRM my population instead of finding the real one. A brief that names a cause spends
the builder's effort on verification, and a wrong named cause spends it on the wrong thing.

### Sequencing

If the flake investigation concludes that two smokes must be serial, **implement the default inversion
as part of that task** rather than filing it forward — the evidence and the mechanism land together.

## Sources

None in this folder. Detail above recovered from the session transcript (`faf7e858-…jsonl`).
