# Brief 535-3 — five gate reds: a leaking restricted PATH and the shifted activity order

## In plain words

The gate shows five drives failing. One clue unlocks most of it: a
neighboring smoke died with "setsid not found" while /usr/bin/setsid
exists — some PATH restriction escaped its intended scope. And the old
activity-bar smoke still expects the pre-Search item order. Fix the
leaks and the neighbors; behavior of your panel should not change.

## End state (mechanically checkable)

Report newer than this stamp; in your worktree these pass:
`bun scripts/harness/smoke-quickopen-harness.ts`,
`smoke-activitybar-harness.ts`,
`smoke-workspace-layout-isolation-harness.ts`,
`smoke-settings-applied-harness.ts`, and your own
`smoke-workspace-search-harness.ts` — each SOLO and all five in one
concurrent batch (the leak only shows under concurrency if it is
file-system-scoped).

## Evidence (failure dir /tmp/merge-gate-failures.ccd48cd6b5416f57.3453584)

1. quickopen: `Executable not found in $PATH: "setsid"` at
   PtyTestDriver spawn — yet /usr/bin/setsid exists. Your
   smoke-workspace-search-harness.ts:550 symlinks setsid into a
   restricted binaryDirectory for the rg-absent arm, and
   smoke-media-harness.ts:314 has a similar loop. First suspect: the
   restricted PATH (env var, shared fixture dir, or a harness-level
   default) escaping to sibling smokes in the pool. Find the leak
   mechanism and confine it to the one spawned app.
2. activitybar: grid timeout at "the Extensions list selects Git before
   disabling it" — your Search item shifted the activity order; the old
   smoke's expectations (indices/click rows) predate it. Update the
   smoke to the NEW canonical order (this is a legitimate
   design-change expectation update — state it in the report).
3. workspace-layout-isolation + settings-applied ("scrollbarThickness
   paints 1 then 3 real columns (2/3)") + plugin-manifest: status/grid
   timeouts, retried and failed twice. Diagnose after items 1-2 — if
   they share the PATH leak they clear together; whatever remains gets
   the three-clocks walk (which clock does each wait read?).

## The bar

Drive first; no timeout widening; expectation updates only where the
DESIGN changed (item 2) and argued in the report. Your own smoke's
restricted-PATH arm must still prove the rg-absent behavior after the
confinement fix — both polarities stay.

## Invariants in scope

Unchanged from briefs 535-1/2, plus "Activity bar order is one
persisted sequence" — your report already declares the migration
refinement; item 2 is its smoke-side face.

## Bycatch expected

Unchanged.
