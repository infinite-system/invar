# Brief 509-2 — OpenPty moves home; the ratchet finding is real

## In plain words

The coupling ratchet red is correct: your new channel/SshClient.ts
value-imports OpenPty from the terminal PLUGIN folder. OpenPty now
has three consumers (terminal runtime, harness, your channel) — it
is a shared generator in the wrong home. Move it to the system tier
and the finding dissolves without an allowlist hole.

## The work

1. git mv src/modules/terminal/OpenPty.ts (+ OpenPty.test.ts, and
   OpenPtyBackend.ts ONLY if it is inseparable — judge and say) to
   src/modules/system/. Update all imports.
2. RIPPLE the contracts: [terminal.invariants.md](../../../../src/modules/terminal/terminal.invariants.md) records citing
   terminal/OpenPty paths ("A live PTY read is readiness driven",
   "One openpty allocator serves both PTY roles", "Shared PTY writes
   never block the event loop") get their Scope/Evidence paths
   corrected — path corrections only, no semantic edits (those are
   refines the conductor reviews). The in-file invariant: annotations
   move WITH the file; run the checker (--all --refs) to zero.
3. Census to zero: bun .invar/tasks/completed/488-core-to-plugin-
   coupling-census/census-488-imports.ts -> offending count 0.
4. Re-run your localhost smoke + the terminal smoke (the six-terminal
   ratchet) — the move must be behavior-identical.

## End state

conventions-gate PASS in your worktree; checker 0 problems; both
smokes green; report updated (newer than this filing stamp) with the
move rationale one-paragraph.

## Invariants in scope

The three terminal records above (path ripple; verdicts stay upheld)
+ The terminal is a runtime plugin (the runtime keeps consuming
OpenPty through the system tier — say so). Answer record by record.

## Rules

Never run scripts/merge-gate.sh; worktree commits skip the gate via
the planted policy; the conductor re-gates and lands.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy even when None observed.

## Instrument feedback

EASY / CONFUSING / MISSING; asks get converted.
