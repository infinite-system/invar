# Addendum — #294: multi-workspace contract coverage + the multi-tsconfig hypothesis

User refinements (2026-07-29 ~17:0x), fold into the same round:

1. **Contract coverage**: the driven tests must exercise LSP load
   across MULTIPLE workspaces in one session — not one secondary
   workspace once. At minimum: default + two secondaries, opens and
   switches interleaved, structure + hover types asserted in EACH.
   The single-workspace assumption in the existing smokes is exactly
   how this class survived.
2. **Ranked-suspect addition (user hypothesis, test it explicitly)**:
   the failing real-world project has MULTIPLE tsconfigs in subfolders
   (e.g. app/ and api/ each with their own dependency tree). Build the
   reproduction fixture BOTH ways: single root tsconfig AND
   monorepo-style (two subfolder tsconfigs with distinct deps). If the
   monorepo shape alone reproduces while the flat shape works, the
   generator is tsconfig DISCOVERY/project-selection (nearest-config
   per file), not just the workspace root — fix must select the
   nearest owning tsconfig per opened file, per workspace.

Both fixture shapes become permanent contract arms either way — the
one that reproduces proves the fix; the other pins the working case.

## Invariants in scope

Unchanged from the dispatch brief; the answers-or-declines record still
binds.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy. The READY report
carries `## Bycatch` even if it reads `None observed`.
