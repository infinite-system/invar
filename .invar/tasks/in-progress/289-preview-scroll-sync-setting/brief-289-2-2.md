# Brief — #289 addendum (round 2, filed early): the preview pane also gets SCROLLBARS

User verification of landed #286 (2026-07-29 12:3x): TOC sync and
jump-into-view work; but the markdown PREVIEW pane projects NO scrollbars
at all — a gap no task owned until now. It is yours because it is the
same pane and the same scroll seam as your sync work.

Add: the preview pane projects vertical (and horizontal, when content
overflows) scrollbars through the SHARED SolidThumbScrollBar painter —
the [#282 record](../../../../src/modules/ui/ui.invariants.md) "One
scrollbar painter gives each axis equal visual weight" governs (thin ▄
horizontal, background-fill vertical, min z-index; continuous drag must
work — the gated ScrollbarThumbDrag assertions extend to the preview's
bars). Wheel, drag, track-click parity with the editor's bars, and your
leadership rule applies: a preview scrollbar drag is preview-led sync.

This folds INTO your round-1 scope — one READY report covers sync + the
preview bars; the drag arm joins the gated scrollbar smoke.

## Invariants in scope

- Round 1's set; the one-painter record; the drag continuity contract.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy. The READY report carries `## Bycatch` even if
it reads `None observed`.
