# Round 3 — #289: merge main into your branch; resolve the markdown-seam conflicts; commit through the gate

Your combined round-2 work is accepted on content. Two blockers before
landing, both mechanical:

1. **Main moved past your base.** #286 (TOC click drives preview) and
   #287 (metadata blocks + H1 color) landed into the markdown seam after
   you cut. `git merge main` (main is at `2a51fb31`) conflicts in:
   - [smoke-markdown-harness.ts](../../../../scripts/harness/smoke-markdown-harness.ts)
   - [markdown.invariants.md](../../../../src/modules/markdown/markdown.invariants.md)
   Resolve by UNION of intent, not either side: their TOC-jump and
   metadata-block contracts must still pass alongside your scroll-sync
   and scrollbar contracts; the invariants file keeps both sets of
   records under the one split-view invariant. Re-run the markdown and
   scrollbar harnesses after resolution.

2. **Your scrollbars commit (`6bfbdc6f`) was made with `SKIP_GATE=1`** —
   it carries no gate verdict. The merge-resolution commit must go
   through the pre-commit hook NORMALLY (no SKIP_GATE), so the full
   merge-gate runs and the landing has a readable verdict. If the gate
   reds on something unrelated to your seam, quote the red verbatim in
   the report and stop — do not re-run with SKIP_GATE.

Update the READY report: note the merge, the resolution shape (which
contracts from each side survived where), and the gate outcome. Keep the
worktree clean.

## Invariants in scope

The markdown split-view records in
[markdown.invariants.md](../../../../src/modules/markdown/markdown.invariants.md)
and the scrollbar painter records in
[ui.invariants.md](../../../../src/modules/ui/ui.invariants.md)
— both sides' records survive the union; no record deleted in resolution.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy. The updated READY
report carries `## Bycatch` even if it reads `None observed`.
