# Brief 546-2 — remove the committed the BUILDER-FUNDAMENTALS priming file from the branch

## In plain words

Your label work is correct and landing-ready, but you committed
the BUILDER-FUNDAMENTALS priming file into the branch — that is the dispatcher's
priming file, injected per-worktree, and it must never enter main
(2814 lines). Remove it from the branch history so only your real
change remains.

## End state (mechanically checkable)

`git diff <merge-base>..HEAD --stat` shows NO the BUILDER-FUNDAMENTALS priming file
(and no [the AGENTS priming file](../../../../the AGENTS priming file)); your git/diff files remain. `git status` clean.

## The fix

`git rm --cached BUILDER_FUNDAMENTALS (the priming file)` then commit the removal (a new
commit is fine — the branch is never rebased under you). Confirm
[the AGENTS priming file](../../../../the AGENTS priming file) is also not in the committed diff (it is dispatcher-owned
too). Leave the file on disk (it primes you); just untrack it.

## The bar

No other change. Re-confirm the label smokes still pass after the
untrack (they will — the file is not code).

## Invariants in scope

Unchanged from brief 546-1.

## Bycatch expected

Unchanged.
