# Round 2 — #281: your change breaks the status-gear settings overlay; gate is RED

The full merge-gate on main+your-branch is RED. This is yours, proven by
control, not load:

- Gate (merged tree, pool): `smoke: editor harness` timeout, retry did
  not rescue — `Timed out waiting for status condition:
  status.settingsOpen === true` in the arm
  `palette and status gear open their overlays` (F1 palette PASSED;
  the status-gear click never opened Settings).
- Solo re-run on the merged tree: same failure, third reproduction.
- Control on unmodified main `a8051148`: `smoke-editor-harness` ALL-PASS.

Logs: `/tmp/gate-281-1401.log`, `/tmp/merge-gate-failures.295620/`,
`/tmp/editor-solo-281.log`, `/tmp/editor-solo-main.log`.

Suspects to check first (yours, from this round): the gear/shared
context-menu gesture in
[StructurePaneContent.ts](../../../../src/modules/structure/StructurePaneContent.ts)
and the contributed-setting write in
[StructurePlugin.ts](../../../../src/modules/structure/StructurePlugin.ts)
— something in that path swallows or shadows the STATUS BAR gear's
overlay open (shared menu registration? capture-phase click handler?
overlay one-owner rule?). Diagnose by driving the failing arm in your
worktree, find the generator, fix at the seam — do not touch the editor
harness's wait or any timeout.

Also note: your branch base predates #291's landing; merge current main
(`a8051148`) while you are at it and re-run. When fixed, commit through
the pre-commit hook NORMALLY (no SKIP_GATE) so the landing has a
readable verdict — your round-1 SKIP_GATE commits are why the conductor
had to run this gate at all.

Update the READY report with the diagnosis, the fix, both polarities
(status gear opens Settings; structure gear still opens the depth menu),
and the gate outcome.

## Invariants in scope

The overlay/one-owner and status-bar records in
[ui.invariants.md](../../../../src/modules/ui/ui.invariants.md); your new
gear records in
[structure.invariants.md](../../../../src/modules/structure/structure.invariants.md).

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy. The updated READY
report carries `## Bycatch` even if it reads `None observed`.
