# Brief 529-2 — the verification section holds placeholders, not results

## In plain words

Your report's diagnosis is excellent. But three verification lines say
FINAL_CONTENTION_RESULT, BUN_TEST_RESULT, and CONVENTIONS_RESULT — the
template words, not the numbers. A claim without its result is not a
verification. Run them and write what actually happened.

## End state (mechanically checkable)

The report file updated (newer than this brief's filing stamp) with the
three placeholder tokens replaced by real results: the 5x4 contention
tally (n green / n total, and each failure's wait if any), the bun test
pass/fail counts, and the conventions-gate verdict — each from runs on
your final committed tree (7a355925 or later).

## The bar

If any of the three runs is red, the report goes back to work, not to
prose: fix or explain with evidence. Do not file the update until the
numbers in it come from commands you actually ran on the final tree.

## Invariants in scope

Unchanged from brief 529-1.

## Bycatch expected

Unchanged; extend your existing section only if the reruns surface
something new.
