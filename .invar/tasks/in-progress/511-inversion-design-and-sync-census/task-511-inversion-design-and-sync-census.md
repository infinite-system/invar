# Task 511 — M4: the inversion design doc + sync-call census (AFTER 509)

Priority: user-directed
Engine: claude
Environment: linux
Model: fable-5
Effort: medium
State: IN-PROGRESS

DESIGN ONLY: how the 12 capability classes become remote proxies over
the 509 protocol (local Invar, remote fs/git/pty/LSP), with the
synchronous-call census attached (the migration bill, stamped with
its commit). The wave draft (sibling 508 folder) M4 section governs.
