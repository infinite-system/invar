# Brief 356-2 — the relocated agent chord lost its reach; restore it

## In plain words

Your plugin move is landed-quality except one regression the full
gate found: Ctrl+Shift+A no longer opens an agent pane while a panel
(terminal) is focused. Before the move, the hand-wired chord fired
anyway; the contributed binding is swallowed by the focused panel.
Two workspace smokes time out on exactly this. Restore the chord's
previous reach without reintroducing a core special case.

## Reproduce by DRIVING first

Your merged tip b307c839 (main merged in; a panelSpace declaration
was added to AgentPlugin for the #405 seam — read that diff first):

- bun scripts/harness/smoke-workspace-tabs-harness.ts — times out at
  "a new agent belongs only to the second workspace" right after the
  smoke types into a terminal and presses Control+Shift+a.
- bun scripts/harness/smoke-workspace-layout-isolation-harness.ts —
  same shape at "workspace A adds an agent pane".
Drive it by hand too: focus a terminal, press the chord, watch
nothing happen.

## The repair direction

The keybinding system needs the chord to act at the app tier even
when a panel owns input — whatever generic mechanism exists or is
cleanest: a reserved or global flag on contributed bindings that the
panel router respects, matching how other app-tier chords (Ctrl+J?)
already pass through. Make it a SEAM capability any plugin can use,
not an agent special case (task #498 tracks the general pass-through
policy; your change should be the mechanism it builds on, and its
task file gains a pointer if you touch shared routing).

## Also fix in the same round (gate findings on your branch)

Six enforced file-grammar violations:
- AgentPlugin.ts + SettingSpecs.ts: missing colocated tests (write
  real ones — the panelSpace declaration and settings specs are
  testable surfaces, no vacuous assertions).
- AgentProviderRegistry.ts: eponymous class first after imports;
  types below the namespace manifest.
- NarrationProjection.ts: namespace manifest directly after the
  class; only exported type aliases and interfaces after it.

## End state (mechanically checkable)

Both named smokes ALL-PASS on your tip, conventions-gate PASS, and a
report file newer than this brief's filing stamp answering: what
mechanism now carries the chord through, and its invariant verdicts
(Focus owns the keystroke needs a refinement proposal if your
mechanism adds a reserved tier — propose wording, do not edit).

## Invariants in scope

- Focus owns the keystroke ([src/modules/keybindings/keybindings.invariants.md](../../../../src/modules/keybindings/keybindings.invariants.md)) — your mechanism refines it; propose wording.
- A focused panel routes keystrokes to its active pane content ([src/modules/ui/ui.invariants.md](../../../../src/modules/ui/ui.invariants.md)) — pass-through must not gut it.
- Bindings are intent addressed ([src/modules/keybindings/keybindings.invariants.md](../../../../src/modules/keybindings/keybindings.invariants.md)).
- The agent pane is a PaneContent citizen, not a special case ([src/modules/agent/agent.invariants.md](../../../../src/modules/agent/agent.invariants.md)) — no core special case may return.
Answer record by record; name misses.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy even when None observed.

## Instrument feedback

EASY / CONFUSING / MISSING as usual.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 for commits; the
conductor re-gates and lands.
