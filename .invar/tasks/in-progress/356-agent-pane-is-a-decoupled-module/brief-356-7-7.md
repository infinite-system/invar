# Brief 356-7 — two more deterministic reds; the inventory grows to the diff's whole surface

## In plain words

Round 6's inventory was still my sample, not the surface: two more
smokes fail deterministically on your tip. Fix both, then run the
name-primary inventory below — every smoke whose subject intersects
your diff's modules (agent, narration, terminal, panel/pane/popup,
keybindings, settings, workspace). Report once with the full table.

## Failing now (deterministic, solo, on edc316c9 merged)

1. bun scripts/harness/smoke-voice-picker-harness.ts
   -> Timed out: "the Narration voice row publishes its automatic
      initial value". Round 6 restored narration setting identifiers;
      the voice row's automatic initial value (dynamic enum from
      VoiceDiscovery) is likely not published on enabled boot, or the
      row publishes under a different identifier now.
2. bun scripts/harness/smoke-terminal-stage-harness.ts
   -> Timed out: findText("terminal command user-executed"). Round 6
      added the decoded-key observer and copy-completion counter;
      something in the terminal stage / observation path lost the
      user-executed stamp, or the agent terminal-follow settings
      rename still bites this path.

## The inventory (bare runs; TS via bun, .sh via bash)

All of round 6's fifteen, PLUS:
- smoke-voice-picker-harness.ts
- smoke-terminal-stage-harness.ts
- smoke-bounded-list-popup-harness.ts
- smoke-settings-applied-harness.ts
- smoke-terminal-harness.ts
- smoke-terminal-follow-harness.ts
- smoke-terminal-backpressure-harness.ts
- smoke-panel-chrome-harness.ts (solo; its contention-tier red is
  #500 — if solo-red on your tip, fix or prove merge-base)

Per-smoke table again: GREEN / FIXED-THEN-GREEN (mechanism) /
RED-OUTSIDE-DIFF (merge-base proof).

## Invariants in scope

The running set. Answer record by record for new repairs.

## End state

Every inventory command exits 0 on your tip (or carries a merge-base
proof); report newer than this filing stamp.

## Bycatch expected / Instrument feedback

As always.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 commits; the conductor
re-gates and lands.
