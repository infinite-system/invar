# Brief 356-8 — the shortcut sheet cannot page past row 94

## In plain words

One blocking red left in the whole gate: the shortcut help sheet
stops advancing at row 94 while the smoke seeks Toggle Word Wrap.
Deterministic solo on your tip. Your chord relocations changed the
sheet's content and likely its length or paging math (a fixed
page-count, a stale total-row bound, or a category that no longer
renders). Fix it; everything else is green.

## Reproduce EXACTLY

bun scripts/harness/smoke-shortcut-help-harness.ts
-> Timed out: "PageDown advances the shortcut sheet beyond row 94
   while seeking Toggle Word Wrap"

## End state

That smoke ALL-PASS bare on your tip; spot-rerun the inventory
smokes your fix could plausibly touch (keyboard invariant,
panel-split); report newer than this filing stamp.

## Invariants in scope

The running set; answer for the repair only.

## Bycatch expected / Instrument feedback

As always.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 commits; the conductor
re-gates and lands.
