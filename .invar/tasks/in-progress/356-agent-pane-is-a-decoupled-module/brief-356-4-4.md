# Brief 356-4 — the split order fix does not hold when the smoke runs itself

## In plain words

Round 3's report says the keyboard smoke passes with a fresh home,
but running the smoke exactly as the gate runs it fails now, every
time: the split paints 'Terminal,Agent', wanted 'Agent,Terminal',
and the follow-mode chord then dies downstream of the wrong split.
Fix it for the smoke AS IT RUNS, not for a hand-prepared home. One
more surface: the panel-split smoke's nerd arm times out waiting for
the status bar to expose terminal and agent controls.

## Reproduce EXACTLY like the gate

In your worktree, run without any preparation:

1. bash scripts/smoke-keyboard-invariant.sh
   -> FAIL Ctrl+Shift+S split the panel (panelCellLabels stayed
      'Terminal,Agent', wanted 'Agent,Terminal')
   -> FAIL Ctrl+Shift+M (terminalFollowMode never left 'off')
2. bun scripts/harness/smoke-panel-split-harness.ts
   -> Timed out: nerd status bar exposes terminal and agent controls

Diagnose why your priority mechanism holds in your probe but not in
the smoke's own environment: read what home/profile the smoke builds,
what panelContentOrder it starts from, and in what order the runtimes
register there. The difference between your green and this red IS the
finding — name it in the report before fixing.

Also check contention: panel-chrome failed on the contention tier in
the same gate (log /tmp/merge-gate-failures.8bee3f81d3ec34aa.1050976).
If your diff plausibly touches its waits, fix; if not, say so with
evidence.

## Invariants in scope

Same set as round 3 (Focus owns the keystroke refinement pending;
panel order records). Answer record by record.

## End state (mechanically checkable)

scripts/smoke-keyboard-invariant.sh exits 0 run bare from the
worktree root; smoke-panel-split-harness.ts ALL-PASS; rounds 2-3
smokes still green; report newer than this filing stamp naming the
environment difference that fooled round 3.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy even when None observed.

## Instrument feedback

EASY / CONFUSING / MISSING as usual.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 commits; the conductor
re-gates and lands.
