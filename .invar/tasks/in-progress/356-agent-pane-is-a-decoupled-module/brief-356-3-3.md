# Brief 356-3 — the plugin move dropped part of the agent pane's behavior surface

## In plain words

Round 2 fixed the toggle-open chord, but the full gate drives more of
the agent surface, and four smokes show behavior the plugin move lost:
the split chord, the follow-mode chord, toggle-CLOSE, first-split
ordering, and the panel scroll surface. Restore each driven behavior
exactly; the smokes name the wanted end states.

## The failing evidence (gate on 01c957aa, logs preserved at
/tmp/merge-gate-failures.8bee3f81d3ec34aa.949895)

1. scripts/smoke-keyboard-invariant.sh:
   - "Ctrl+Shift+S split the panel" — panelCellLabels stayed
     'Terminal,3D Demo', wanted 'Agent,Terminal'.
   - "Ctrl+Shift+M arrived at the focused agent pane" —
     terminalFollowMode never left 'off'.
   - "Ctrl+Shift+A closed the agent pane" — panelCellLabels stayed
     'Agent', wanted 'Terminal' (toggle must CLOSE on second press).
2. smoke-activitybar-harness.ts: "the fresh profile paints agent left
   of terminal on its first split" — timed out (ordering within the
   shared Terminal space).
3. smoke-clipboard-frame-boundary-harness.ts: "the panel scroll
   surface is active before clipboard input" — timed out.

## Reproduce by DRIVING first, one smoke at a time

Run each failing smoke on your tip; fix; re-drive; next. Suspects
from the diff: chords you did not relocate (split, follow-mode) or
relocated without the applicationGlobal/context they had; toggle
semantics reduced to open-only; pane insertion order now
registration-driven where the hand-wiring put agent first; a scroll
surface registration that the generic route no longer performs.

## Invariants in scope

Same set as round 2 (Focus owns the keystroke; A focused panel routes
keystrokes; Bindings are intent addressed; PaneContent citizen) plus:
Panel content order is one persisted sequence ([src/modules/ui/ui.invariants.md](../../../../src/modules/ui/ui.invariants.md))
for the first-split ordering. Answer record by record; name misses.

## End state (mechanically checkable)

All four named smokes ALL-PASS on your tip, plus the round-2 pair
still green, and a report newer than this filing stamp.

## Bycatch expected

Per [AGENTS.md](../../../../AGENTS.md)'s taxonomy even when None observed.

## Instrument feedback

EASY / CONFUSING / MISSING as usual.

## Rules

Never run scripts/merge-gate.sh; SKIP_GATE=1 commits; the conductor
re-gates and lands.
