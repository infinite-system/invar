# Coverage deltas

This refactor removes clock-bound absence claims only where the claim was
duplicated, unsound, or superseded by a stronger condition/content assertion.

| File | Delta and replacement |
| --- | --- |
| `scripts/behavioral-contracts.sh` | assertions 41 → 41, waits 34 → 34. The smoothness drive removes only the redundant 26,635-line interior sample; its blocking population still spans both editor and diff at the shared 2,000- and 100,000-line scale endpoints, and the exact endpoint work ratios remain authoritative. Glide-cap coverage still drives all three selectable caps at both endpoints. No blocking assertion, positive control, surface, cap, or scale boundary was removed. |
| `scripts/smoke-agent-pane-ux.sh` | RETIRED AND PARKED in the standard retirement directory. Baseline exit 1 confirmed its long-form footer copy and row assumptions had already rotted. The gated `scripts/harness/smoke-agent-pane-ux-harness.ts` is a strict behavioral superset: agent chrome, permission cycling, busy/waiting state, tool collapse, transcript/composer wrapping, scrolling/tail behavior, selection/copy, and duplicate-idle ownership all remain covered. Its planted permission-toggle defect exited 1, so no unique property is lost. |
| `scripts/smoke-agent-engine-switch.sh` | RETIRED AND PARKED in the standard retirement directory. Baseline exit 1 confirmed its global long-form engine-copy locator was stale. The gated `scripts/harness/smoke-agent-engine-switch-harness.ts` is a strict behavioral superset: boot identity/title, keyboard and pointer switching, conversation context transfer, both provider directions, and retained per-message engine attribution remain covered; duplicate-idle ownership is centralized in the behavioral contract. Its planted pointer-dispatch defect exited 1, so no unique property is lost. |
| `scripts/smoke-agent-permissions.sh` | RETIRED AND PARKED in the standard retirement directory. Baseline exit 1 confirmed its retired permission labels were stale. The gated `scripts/harness/smoke-agent-permissions-harness.ts` is a strict behavioral superset: ask/bypass cycling, allow, deny, always-allow, stray-input rejection, prompt identity, and completed-tool absence all remain covered; duplicate-idle ownership is centralized in the behavioral contract. The shared agent-footer permission positive control exited 1 in `smoke-agent-pane-ux-harness.ts`, and the permissions harness already carries its own gated behavioral controls, so no unique property is lost. |
| `scripts/smoke-diff-overview.sh` | RETIRED AND PARKED in the standard retirement directory. Baseline exit 6 confirmed its fixed toolbar rows, workspace offset, and textual `Next` locator were stale. The gated `scripts/harness/smoke-diff-overview-harness.ts` is a strict behavioral superset: proportional top/middle/bottom ruler paint, exact toolbar actions, change navigation, persistent split drag, held-edge selection/copy, and Open-current promotion all remain covered. Its planted next-change defect exited 1, so no unique property is lost. |
| `scripts/behavioral-contracts.sh` | assertions 41 → 41, waits 34 → 34. The count-neutral role refinement replaces the editor/diff size-matrix FPS floors with the primary exact 2k-to-100k editor frame-work ratio, retains one diff cadence floor, and retains the fold-dense editor cadence floor as the one editor wall-clock canary. No behavioral assertion or wait was removed. |
| `scripts/harness/SynchronizedOutputQuiescence.test.ts` | assertions 11 → 10, waits 6 → 5. Removed the duration-based no-frame test with the deleted silence API. Idle frame efficiency remains covered once by `scripts/behavioral-contracts.sh`; action stability is covered by `PtyTestDriver` content-invariance tests. |
| `scripts/harness/smoke-agent-engine-switch-harness.ts` | assertions 11 → 10, waits 16 → 16. Removed its duplicate idle frame-budget assertion. The single idle-quiescence behavioral contract remains authoritative. |
| `scripts/harness/smoke-agent-harness.ts` | assertions 11 → 10, waits 8 → 8. Removed its duplicate idle frame-budget assertion. The single idle-quiescence behavioral contract remains authoritative. |
| `scripts/harness/smoke-agent-pane-ux-harness.ts` | assertions 39 → 38, waits 34 → 36. The pane-local heading and border no longer exist, so the smoke removes assertions that measured composer wrapping from those frame cells. It now asserts the absent local heading, flat blank rows, the visible words, right gap, transcript thumb, selection, copy, input behavior, and assistant-reply copy with a live composer draft through legacy and Kitty Ctrl+C and Cmd+C. `PanelTabBar.test.ts` and the panel-chrome smoke cover the replacement host chrome. |
| `scripts/harness/smoke-agent-permissions-harness.ts` | assertions 8 → 7, waits 16 → 16. Removed its duplicate idle frame-budget assertion. The single idle-quiescence behavioral contract remains authoritative. |
| `scripts/harness/smoke-agent-search-harness.ts` | assertions 10 → 9, waits 19 → 19. Removed its duplicate idle frame-budget assertion. The single idle-quiescence behavioral contract remains authoritative. |
| `scripts/harness/smoke-audio-narration-harness.ts` | assertions 8 → 9, waits 14 → 13. Removed its duplicate idle frame-budget assertion and silence wait. Ordinary typing now proves stable transcript content and changed composer content; idle efficiency remains in the behavioral contract. |
| `scripts/harness/smoke-editor-harness.ts` | assertions 36 → 35, waits 15 → 15. Removed its duplicate idle frame-budget assertion and an untouched-selection silence interval. The former remains in the behavioral contract; the latter was unsound because a legitimate repaint says nothing about selection state, which is still asserted directly. |
| `scripts/harness/smoke-find-harness.ts` | assertions 3 → 3, waits 6 → 7. Removed an optional post-action silence wait and its terminal-dependent replace-all attempt. Ctrl+H opening and Escape closing remain driven here; replace-all mutation remains covered through the real mouse control in `scripts/harness/smoke-search-mouse-harness.ts`. |
| `scripts/harness/smoke-git-watch-harness.ts` | assertions 4 → 4, waits 7 → 8. Removed the 600 ms silence claim after attempting to open a confined directory symlink. The action is intentionally a no-op, so no changed-region liveness control exists; the smoke now proves the app remains live by opening Quick Open and retaining the Git change count. |
| `scripts/harness/smoke-hover-harness.ts` | assertions 5 → 4, waits 8 → 7. Removed the sub-dwell duration assertion. It measured scheduler load rather than hover behavior; the positive condition that the completed dwell renders the hover remains. |
| `scripts/harness/smoke-markdown-harness.ts` | assertions 13 → 13, waits 31 → 30. Removed the selection-release silence wait. Immediate copy now waits for the named copied-selection status, proving both preserved selection and action liveness. |
| `scripts/harness/smoke-terminal-harness.ts` | assertions 24 → 23, waits 27 → 27. Removed its duplicate idle frame-budget assertion. The single idle-quiescence behavioral contract remains authoritative. |
| `scripts/harness/smoke-word-delete-harness.ts` | assertions 7 → 8, waits 14 → 13. Replaced the post-delete silence wait with a required stable tree region, a required changed editor region, and the existing exact text/cursor assertions. |
| `src/modules/theme/ThemeIcons.test.ts` | assertions 23 → 17, waits 6 → 7. Nine per-slot `glyphFor` equality assertions were replaced by one whole-vocabulary `toEqual` per tier, which is STRICTER — it catches a missing or extra slot, which nine individual checks could not — plus two new per-glyph property assertions (exactly one display cell; no collision with the reserved `▎`/`●`/`❯` marks) that hold for any future vocabulary rather than for these glyphs only. Fewer calls, more covered. |
| `scripts/harness/smoke-panel-chrome-harness.ts` | assertions 25 → 29, waits 46 → 67. The panel overhaul moves document actions to the editor border, gives the tab row its own plugin and instances controls, removes the drag-strip paint pad, and adds draggable tabs. The smoke now drives those gestures, exact splitter crossing and color cells, old pane-ID restore, two equal-name Database panes through creation and separate removal, the empty-panel Add affordance, direct instance close, and counted container close in the real PTY. |
| `scripts/harness/smoke-navigation-history-harness.ts` | assertions 9 → 17, waits 17 → 40. The union keeps the file-to-comparison-to-file trail, cursor restoration, and both padded breadcrumb buttons. It also drives the xterm, macOS readline, and fallback byte forms through every intermediate comparison state, and checks the project, breadcrumb, file-tab, and editor tones. |
| `scripts/harness/smoke-terminal-stage-harness.ts` | assertions 24 → 24, waits 37 → 36. The removed waits observed the deleted terminal heading and its live working-directory label. The drive now proves that no local heading remains and runs `pwd` after `cd /tmp` through the real terminal to prove the same working-directory state. One repaint wait returns to distinguish the latest terminal tool result from an earlier visible result before the drive clicks it. |
| `src/modules/ui/PanelHeading.test.ts` | assertions 14 → 0, waits 5 → 0. The panel-heading component was removed with per-pane chrome. `PanelTabBar.test.ts` replaces its paint and hit-geometry coverage for the workspace tab row, while `scripts/harness/smoke-panel-chrome-harness.ts` drives the shared controls in the real PTY. |
| `src/modules/ui/PanelSeparatorRow.test.ts` | assertions 12 → 0, waits 2 → 0. The separator component was removed because the workspace tab row is now the panel's only chrome. `PanelTabBar.test.ts` replaces its projection and hit-geometry coverage, and the panel-chrome smoke retains the gap-free action-drag-control and centered splitter-paint claims on the new row. |
| `src/modules/ui/PanelContentsList.test.ts` | assertions 19 → 18, waits 4 → 4. The removed assertion covered the deleted pane-heading close glyph. The test retains the buffer-tab close glyph and the management-list close glyph, while `PanelTabBar.test.ts` proves the panel-level close token at every tier. |
| `scripts/harness/smoke-database-harness.ts` | assertions 8 → 7, waits 24 → 25. Repeated locked and corrupt path-input drives were removed when content spaces exposed #395 (Database connect hidden-field focus). The repaired smoke removes its manual Database-tab click. One compound status wait now requires the owning Database space, focused input, and painted Database cell, and a grid wait requires the visible path field. It retains real connect, reconnect, disconnect, paging, provider lifecycle, and missing-file failure. |
| `scripts/harness/smoke-media-harness.ts` | assertions 20 → 21, waits 24 → 23. The removed wait observed a title deleted with pane-local chrome. The same scene change is observed through `mediaScene`, while pixel fingerprints prove the selected scene remains painted and changing. |
| `scripts/harness/smoke-terminal-backpressure-harness.ts` | assertions 6 → 5, waits 6 → 7. A `requireCondition` that sampled the grid after `awaitQuiescence()` became an `awaitGridCondition` on the same text. Quiescence means "no frame is pending", which is NOT what the assertion read: under gate load the shell was still starting, so the heading had not been painted and the sample found nothing. It failed a gate on 2026-07-26 while passing solo. The claim is unchanged and now load-robust — one assertion became one wait that observes exactly what it asserts. |
| `scripts/harness/smoke-scrollbars-harness.ts` | assertions 36 → 35, waits 20 → 22. Two thumb-PRESENCE claims sampled a snapshot whose preceding wait observed the FILE TEXT, not the thumb: `editor vertical thumb is present with ${modeLabel}` and `editor horizontal thumb is present`. The debug-bars probe paints the thumb in a later frame under gate load, so both failed inside gates on 2026-07-26 while passing solo — and they fail HARD rather than timing out, so retry-once could not even mask them. Each now awaits the thumb it asserts; the horizontal site keeps its assertion on the awaited snapshot, so only the vertical one traded an assertion for a wait. |
| `scripts/harness/smoke-git-blame-harness.ts` | assertions 7 → 7, waits 10 → 11. The post-reconcile blame assertion remains unchanged; its preceding wait now observes both the watched-tree refresh and the asserted empty blame author. |
| `scripts/harness/smoke-layout-harness.ts` | assertions 43 → 43, waits 44 → 46. Both status-edge assertions remain; their preceding grid wait now observes the exact clock and right-dock affordance text they read. |
| `scripts/harness/smoke-panel-split-harness.ts` | assertions 35 → 29, waits 33 → 31. Per-instance headers now own close actions, while the shared splitter keeps only expand and close. The smoke retains real split creation, focus routing, child PTY sizing, and divider reflow, then adds the instances chip, hover-only controls, direct instance close without a dialog, counted container confirmation, and the in-place Agent replacement drive. |
| `scripts/harness/smoke-terminal-follow-harness.ts` | assertions 16 → 16, waits 44 → 43. The intentionally removed terminal-follow footer control no longer needs a grid wait to locate `follow: off`; the same live setting remains driven through Ctrl+Shift+M and the command palette, while the replacement footer wait proves the compact chrome omits the retired control. |
| `scripts/harness/smoke-terminal-harness.ts` | assertions 23 → 24, waits 27 → 31. Scroll-frame sampling now awaits changed row offsets, wheel reversal awaits the exact directional transition, and the child-wheel external boundary is observed over a deadline-bounded unchanged-state window. |
| `scripts/harness/smoke-text-input-harness.ts` | assertions 11 → 11, waits 16 → 17. The no-drill assertion now follows a status wait for the exact retained quick-open query it reads. |
| `scripts/harness/smoke-workspace-tabs-harness.ts` | assertions 27 → 27, waits 27 → 28. The tab-switch assertions retain their exact status claims and now receive the status snapshot from a matching combined wait. |
| `src/modules/ui/GitPaneRenderer.test.ts` | assertions 1 → 0, waits 1 → 0. Moved unchanged to `src/modules/git/GitPaneRenderer.test.ts`, which now carries those counts; the assertion remains colocated with the behavior it proves. |
| `src/modules/workspace/GitPanel.test.ts` | assertions 21 → 0, waits 5 → 0. Moved unchanged to `src/modules/git/GitPanel.test.ts`, which now carries those counts; every panel assertion survives at the new path. |
| `src/modules/workspace/Workspace.gitRaces.test.ts` | assertions 17 → 0, waits 4 → 0. Replaced by `src/modules/git/GitWorkspace.races.test.ts` and `src/modules/git/GitDocumentState.test.ts`: async branch/diff supersession now drives the plugin controller, and the former shared-active-head assertions are superseded by the stronger per-handle counterfactual (an unkeyed `byLine()` call is a `@ts-expect-error`, so the bug class cannot be written rather than merely being tested for). |
| `src/modules/workspace/Workspace.scroll.test.ts` | assertions 25 → 5, waits 9 → 3. Repository-pane scroll ownership moved out of `Workspace` into `GitWorkspace`, so the host test retains only editor/tree momentum and live-settings claims. The moved claims live in the `GitWorkspace`/`GitPanel` tests and the three-run log smoke. |
| `src/modules/workspace/WorkspaceSet.test.ts` | assertions 17 → 15, waits 3 → 3. Removed two redundant aggregate-count assertions. Each switch still asserts the COMPLETE per-workspace watcher vector, which strictly determines the aggregate one-live-watcher count and additionally identifies which workspace owns it. |
| `src/modules/theme/ThemeIcons.test.ts` | assertions: 24 → 41, waits: 10 → 16 — the symbol-mark table replaced the extension-keyed icon sets, so this file gained the whole-table pin per tier (stricter than per-class checks: it catches a missing OR extra class), the one-cell and pairwise-distinctness claims for the code-symbol families including the ascii rung, the filesystem classifier's cases, and the app-versus-terminal width-agreement instrument with a wide-glyph positive control. The reserved-mark claim stopped pasting a literal list and now reads the recorded owner table. |
| `src/modules/lsp/CompletionItemKinds.test.ts` | assertions: 0 → 10, waits: 0 → 4 — new file. Covers all 25 protocol kinds resolving to a one-cell mark at every tier, an absent kind and a newer-protocol kind classifying rather than vanishing, the family grouping (callables share a mark, a value does not), and the cross-consumer claim that an LSP File or Folder completion carries the mark the file tree paints for the same thing — fetched through the tree's own entry point, so a second resolver would break it. |
| `scripts/harness/smoke-completion-harness.ts` | assertions: 13 → 17, waits: 18 → 20 — the drive now proves the popup PAINTS the marks: the five-thousand-item list reserves exactly one mark cell, and a real tsgo member access is read out of the emulator grid with the callable mark beside `method` and the value mark beside a data member. Rows are located by item TEXT and the marks come from the kind classifier and the theme, never from a pasted glyph; the label column check is the width proof on the real path. |
| `src/modules/ui/BreadcrumbPicker.test.ts` | assertions: 19 → 19, waits: 2 → 2 — no count movement; the icon stub and its expected row now name symbol classes (`directoryClosed`, `typescript`, `file`) instead of `IconSet` fields, because the extension-keyed set no longer exists. Same claim, same strength, new vocabulary. |
| `src/modules/theme/ThemeIcons.test.ts` | assertions: 41 → 48, waits: 16 → 19 — the mark-ownership mechanism. Added: every shared mark is declared and every declaration is still real (so a new sharing fails AND a stale reason fails), a synthetic-list positive control proving the detector can report a collision at all, and the resolved `●` collision stated so it cannot silently return. The extensions-slot and code-symbol claims stopped consulting a pasted reserved list and now ask the ownership table who else means something by the mark. |
| `src/modules/plugins/DefaultPlugins.test.ts` | assertions: 1 → 2, waits: 1 → 1 — the shipped contributor census now proves which default contributors opt into workspace lifecycle. |
| `src/modules/plugins/ExtensionsPlugin.test.ts` | assertions: 2 → 3, waits: 1 → 1 — the application-only Extensions contributor now proves it does not fabricate workspace lifecycle. |
| `src/modules/git/GitPlugin.test.ts` | assertions: 3 → 4, waits: 1 → 1 — the source-control contributor now proves its explicit workspace-contributor port points at the same plugin instance. |
| `src/modules/markdown/MarkdownPlugin.test.ts` | assertions: 13 → 13, waits: 9 → 9 — no count movement; the test host now names the application contribution context. |
| `src/modules/workspace/WorkspaceSet.test.ts` | assertions: 15 → 15, waits: 3 → 3 — no count movement; direct workspace-set construction now supplies workspace contributors. |
| `src/modules/keybindings/KeybindingDefaults.test.ts` | assertions 22 → 45, waits 4 → 15. The keystroke-ownership invariant is now enumerated where it lives: every reserved binding carries a warrant and a modifier, no unmodified key is claimed globally, no `F<n>` is any action's primary, Tab/Shift+Tab resolve to editor indent/outdent while resolving to nothing in the terminal and agent contexts, every retired F-key's replacement resolves, and the host floor names no contributed-surface (`diff.*`) action — the comparison surface owns its own Ctrl+Shift+Up/Down. |
| `src/modules/keybindings/KeybindingRegistry.test.ts` | assertions 45 → 47, waits 17 → 19. Two hint-deliverability claims: a `super` (Cmd) alias never displaces the floor chord in the advertised map, while a user rebind still does. Added because the platform generator briefly made a Linux cheat-sheet advertise `Cmd+P` for Go to File — a chord that cannot arrive for that user. |
| `src/modules/keybindings/KeybindingPlatform.test.ts` | NEW: assertions 15, waits 6. The platform substitution cannot drift: every generated alias has an identical-in-everything-but-`ctrl` floor twin, reservation and warrant survive the substitution, `Ctrl+P` and `Cmd+P` resolve to the SAME action (the pre-generator overlay disagreed), no action is reachable only with `super`, and the hand-written mac residue is exactly the ten chords no substitution can derive. |
| `src/modules/editor/EditorIndent.test.ts` | NEW: assertions 24, waits 0. Indent-unit detection (tabs win; the space step is the smallest positive leading run; whitespace-only lines are not levels; two-space fallback), at-most-one-unit outdent, caret-position indent, block indent/outdent with a surviving selection, empty lines untouched, one atomic undo step, and read-only refusal. |
| `scripts/harness/PtyTestDriver.test.ts` | assertions 45 → 47, waits 26 → 26. `Ctrl+]` (a C0 byte) and `Ctrl+Shift+Up` are now asserted encodings, and the Ctrl+Shift+letter form moved from the kitty-only CSI-u shape to the xterm modifyOtherKeys shape that BOTH OpenTUI parsers decode — the change that makes a Ctrl+Shift drive evidence about legacy terminals rather than about kitty only. |
| `scripts/smoke-keyboard-invariant.sh` | NEW driven smoke. Tab/Shift+Tab indentation at the caret and across a multi-line selection, arrival of all eight replacement chords, the terminal pass-through sent-vs-received sweep (38 chords), and the reserved set still overriding a focused terminal (panel toggle steals with no leak; Ctrl+Q quits from inside the terminal). |
| `src/modules/ui/FileTreePaneContent.test.ts` | assertions 7 → 0, waits 2 → 0. Moved to `src/modules/filetree/FileTreePaneContent.test.ts`, where the plugin-owned pane now carries 10 assertions and 2 waits; all former identity, pointer activation, and viewport claims survive, with generic focus/context and both projected viewport axes added. |
| `src/modules/ui/TreePaneRenderer.test.ts` | assertions 1 → 0, waits 1 → 0. Moved unchanged to `src/modules/filetree/TreePaneRenderer.test.ts`, which carries the same assertion and wait beside the plugin-owned renderer. |
| `src/modules/workspace/FileTree.scroll.test.ts` | assertions 20 → 0, waits 5 → 0. Moved unchanged to `src/modules/filetree/FileTree.scroll.test.ts`, which carries all 20 assertions and 5 waits beside the plugin-owned model. |
| `src/modules/workspace/FileTree.test.ts` | assertions 9 → 0, waits 4 → 0. Moved unchanged to `src/modules/filetree/FileTree.test.ts`, which carries all 9 assertions and 4 waits beside the plugin-owned model. |
| `scripts/harness/smoke-activitybar-harness.ts` | assertions 30 → 31, waits 18 → 18. The default-manifest drive now asserts that Explorer itself is plugin-contributed before comparing the activity-bar and sidebar registries; this would fail if the host silently reintroduced its own Files entry. |
| `src/modules/keybindings/KeybindingRegistry.test.ts` | assertions 47 → 52, waits 19 → 21. Added the explicit host-floor → plugin-default → user-rebind precedence lattice plus refusal and symmetric removal of a plugin reservation claim. |
| `src/modules/settings/Settings.test.ts` | assertions 61 → 58, waits 9 → 10. Removed three assertions for concrete plugin keys from the host schema test. Their persistence and lifecycle claims are replaced at the contributed-setting seam here, in `ApplicationContributions.test.ts`, and by the real Settings/Extensions drive below. |
| `src/modules/settings/SettingsPanel.test.ts` | assertions 35 → 36, waits 7 → 8. Added a contributed row under its supplied section heading and proved disposing the contribution removes it. |
| `src/modules/app/ApplicationContributions.test.ts` | NEW: assertions 6, waits 2. Proves plugin defaults sit above the host floor and below user rebinds, then proves one disable removes both the contributed schema and binding layer. |
| `scripts/harness/smoke-plugin-manifest-harness.ts` | NEW: assertions 9, waits 27. Drives the real PTY through contributed File Tree/Git/Markdown settings headings and a live hidden-file effect; editor-versus-Git Tab ownership; and Extensions uninstall/reinstall removing and restoring settings rows and binding behavior. |
| `src/modules/plugins/DefaultPlugins.test.ts` | assertions 2 → 3, waits 1 → 1. The shipped typed manifest now pins each contributor's stable identifier and user-facing name in composition order. |
| `scripts/harness/QuietLock.test.ts` | NEW: assertions 14, waits 2. Positive controls prove loud-shared holders overlap, a loud-shared child blocks quiet-exclusive work until release, a bounded acquisition timeout warns with the holder name and proceeds, and killing a holder releases the OS lock for the next quiet acquisition. |
| `src/modules/editor/CodeFolding.test.ts` | NEW: assertions 5, waits 4. Covers nested TypeScript brace/bracket regions from syntax roles, delimiter-shaped strings/comments staying non-structural, indentation-only regions, and revision-keyed reuse with a line-read counter that proves unchanged queries do not rescan. |
| `src/modules/editor/Editor.test.ts` | assertions 54 → 64, waits 17 → 19. Adds folded vertical and word movement with selection endpoints skipping the hidden body, direct navigation auto-unfolding it, and fold state reattachment through a stable document handle. |
| `src/modules/editor/EditorWrapIndex.test.ts` | assertions 13 → 19, waits 6 → 8. Adds folded lines contributing zero rows to the existing cumulative index, window/inverse agreement across the collapsed block, hidden and visible document lines resolving through the same visual-row authority, and past-end clamping to the final line that still contributes a row. |
| `src/modules/keybindings/KeybindingDefaults.test.ts` | assertions 44 → 52, waits 15 → 17. Proves the existing workspace Ctrl+Shift bracket bindings remain authoritative while the distinct editor-context fold and unfold step lists both resolve without collision, then decodes both complete byte sequences through OpenTUI's legacy and kitty parser modes. |
| `src/modules/theme/ThemeIcons.test.ts` | assertions 48 → 52, waits 19 → 20. Adds both fold-control slots to every semantic vocabulary pin, proves app and independent terminal width authorities agree on exactly one cell at every tier, and excludes every reserved/editor/activity mark. |
| `src/modules/workspace/OpenBufferSet.test.ts` | assertions 26 → 31, waits 7 → 8. Proves one document handle retains fold state across clean-buffer dehydration/rehydration and that closing then reopening the path creates an empty replacement handle. |
| `scripts/harness/smoke-code-folding-harness.ts` | NEW: assertions 8, waits 21. Drives a real TypeScript file through the exact number-gutter mouse cell, collapsed grid and indicator, mouse unfold, editor-table keyboard fold, caret skip-over, find navigation auto-unfold, and fold-state persistence across a file switch. |
| `src/modules/ui/OverviewRuler.test.ts` | assertions 5 → 6, waits 3 → 4. Keeps identity projection, aggregation priority, and cache claims while making the visual projection explicit, then proves a hidden-body diagnostic lands at its supplied fold-header row rather than at the raw document-line ratio. |
| `scripts/harness/smoke-inline-rewrite-harness.ts` | assertions 0 → 8, waits 0 → 15. New drive covering contributed setting discovery, debounce, themed dim/italic proposal paint, both variation directions, atomic accept/undo, reject, typing-through, stale-response discard, and idle quiescence while the deterministic provider is in flight. |
| `scripts/harness/measure-inline-rewrite-codex.ts` | assertions 0 → 2, waits 0 → 4. New opt-in full-PTY real-Codex latency drive with a known-delay mock positive control and one visible Spark proposal. |
| `src/modules/editor/InlineRewrite.test.ts` | assertions 0 → 10, waits 0 → 4. New coverage for one-request cancellation, revision-stale discard, cyclic ordering, and silent provider-error counting. |
| `src/modules/lsp/CodexRewriteProvider.test.ts` | assertions 0 → 5, waits 0 → 4. New coverage for CLI absence, provider-neutral JSON mapping, JSONL assistant-message framing, and malformed response handling. |
| `src/modules/editor/Editor.test.ts` | assertions 64 → 68, waits 19 → 21. Adds one-step rewrite acceptance/undo and proves ordinary typing dismisses the proposal while its character still lands. |
| `src/modules/keybindings/KeybindingDefaults.test.ts` | assertions 52 → 59, waits 17 → 19. Adds exact inline-rewrite action resolution, guarded Escape, Tab retention, and both-parser delivery for request and modified-arrow chords. |
| `src/modules/theme/ThemePalettes.test.ts` | assertions 3 → 5, waits 3 → 4. Proves both palettes contribute distinct inline-rewrite foreground and background vocabulary slots. |
| `scripts/harness/smoke-scrollbars-harness.ts` | assertions: 36 → 40, waits: 30 → 34 — the diff drive now byte-compares the complete horizontal scrollbar row across every synchronized vertical-scroll frame, then lengthens the widest line through the real editor and proves a refreshed comparison changes the bar. |
| `scripts/harness/smoke-selection-harness.ts` | assertions 15 → 16, waits 14 → 16. The source-control hover drive now proves the decorated row changes while the complete following row remains byte-identical. |
| `scripts/harness/smoke-git-log-harness.ts` | assertions 25 → 29, waits 20 → 29. The log drive single-clicks a commit and its file, arrows through three commits and their files while checking published Git focus and distinct comparison headers, then proves Enter and double-click transfer focus. |
| `src/modules/git/CommitExpansion.test.ts` | assertions 15 → 18, waits 7 → 8. A repeated expansion now proves it joins one in-flight fetch and cannot complete before that shared fetch lands. |
| `src/modules/git/GitWorkspace.races.test.ts` | assertions 2 → 4, waits 2 → 3. The comparison seam now proves preview preserves primary-pane focus and explicit activation transfers focus to the editor. |
| `scripts/harness/smoke-clipboard-frame-boundary-harness.ts` | assertions 5 → 5, waits 12 → 11. Two fixed-distance top/bottom waits were consolidated behind one semantic-anchor driver that is invoked for both anchors and keeps sending real wheel trains until each original predicate is observed; runtime claims are unchanged, while the source census sees the shared wait once. |
| `src/modules/theme/ThemeIcons.test.ts` | assertions 52 → 51, waits 20 → 20. The former subset-specific width assertions and two-entry exception branch are superseded by one exhaustive offender-table assertion over every public theme vocabulary surface at every tier. A failure now reports the semantic entry, glyph, app width, and independent terminal width; the wide-glyph positive control remains, and separate vocabulary-completeness assertions prevent an empty or partial census from passing. |
| `scripts/harness/smoke-bounded-list-popup-harness.ts` | assertions 44 → 46, waits 69 → 69. The real PTY fixture now adds ordinary, image, and `bun.lock` rows and asserts one filename start column in both the file tree and breadcrumb popup. |
| `scripts/harness/smoke-code-folding-harness.ts` | assertions 8 → 15, waits 21 → 38. Adds the contributed `editor.codeFolding` live-disable/enable drive plus deep pointer and keyboard fold/unfold drives that pin the topmost document line and fold-header row around line 530. |
| `src/modules/editor/BracketMatch.test.ts` | assertions 12 → 17, waits 12 → 13. Adds a 10,000-frame revision/cursor/language snapshot ratchet with document-line reads unchanged until a positive-control revision edit. |
| `src/modules/editor/Editor.test.ts` | assertions 72 → 88, waits 22 → 24. Proves folding-off performs zero structural reads while expanding every row, then proves pointer-equivalent and keyboard toggles preserve a deep viewport anchor. |
| `src/modules/editor/EditorWrap.test.ts` | assertions 73 → 80, waits 31 → 32. Adds a 100k collapsed-body projection ratchet: one projection build, O(1) hidden-line and past-end lookups, and only viewport-sized line reads across 10,000 frames. |
| `src/modules/workspace/WorkspaceSet.test.ts` | assertions 15 → 17, waits 3 → 4. Proves the host-owned code-folding capability source attaches to workspace editors and live-applies both boolean states. |
| `src/modules/editor/InlineRewrite.test.ts` | assertions 10 → 0, waits 4 → 0. Moved to `src/modules/inline-rewrite/InlineRewrite.test.ts` with all 10 assertions and 4 waits because the controller is now owned by the extracted plugin. |
| `src/modules/keybindings/KeybindingDefaults.test.ts` | assertions 59 → 52, waits 19 → 17. Removed seven plugin-specific binding and parser claims from the host canonical table; `InlineRewriteContributor.test.ts` now pins plugin-layer request resolution, while `smoke-inline-rewrite-harness.ts` drives request, guarded reject, both variation arrows, accept, and editor Tab through the real PTY. |
| `scripts/harness/smoke-agent-permissions-harness.ts` | assertions 7 → 8, waits 17 → 17. Each gated turn now proves its pending Bash permission belongs to the submitted prompt through semantic session status, while the terminal grid independently proves all actionable approval controls remain visible and no completed tool row appears. |
| `src/modules/agent/AgentSession.test.ts` | assertions 115 → 119, waits 33 → 34. Adds the cancellation-publication mechanism pin: synchronous backend teardown leaves the canceled state intact and schedules exactly one guarded render revision after the teardown stack unwinds. |
| `scripts/harness/smoke-activitybar-harness.ts` | assertions 31 → 38, waits 18 → 31. Adds fresh and planted panel-order profiles, plugin disable/re-enable slot retention, activity-context keyboard reorder, pointer drag, and same-HOME restart persistence. |
| `scripts/harness/smoke-settings-applied-harness.ts` | assertions 24 → 24, waits 24 → 24. No count movement; the settings schema census now records that `primaryDockContentOrder` is driven by the activity-bar harness. |
| `scripts/smoke-settings-applied.sh` | RETIRED AND PARKED in the standard retirement directory. Its opt-in tmux gain check observed one notch, but `scrollAccelGain` shapes successive same-direction impulses. The normal-gate PTY harness supersedes every applied-effect claim, enumerates all 36 runtime schema fields, and drives gain with two impulses. |
| `src/modules/keybindings/KeybindingDefaults.test.ts` | assertions 59 → 63, waits 19 → 20. Proves the activity and bottom-panel contexts resolve the same Alt+Up and Alt+Down reorder gestures. |
| `src/modules/settings/Settings.test.ts` | assertions 58 → 64, waits 10 → 11. Covers the primary-dock order default, shared validation and deduplication, save output, and reload round-trip. |
| `src/modules/ui/ContentOrderDrag.test.ts` | assertions 0 → 4, waits 0 → 1. New file. Proves pointer movement is inert before and after a drag and delegates the active drag to the persisted `PanelHost` reorder seam. |
| `src/modules/ui/PanelHost.test.ts` | assertions 57 → 67, waits 15 → 18. Covers dormant identifiers as inert membership gaps, deterministic unseen-content append, retained disable/re-enable slots, and persisted visible reorder across a dormant identifier. |
| `scripts/harness/smoke-agent-skill-popup-harness.ts` | assertions 7 → 10, waits 13 → 14. The real PTY catalog now uses folded and literal block-scalar descriptions, proves their indicators never reach frame cells, requires a known long description to end in one ellipsis, and checks every visible item row remains inside the popup border. |
| `src/modules/agent/AgentPromptResolver.test.ts` | assertions 5 → 7, waits 5 → 7. Covers plain, quoted, folded, and literal description scalars; both chomping directions; explicit indentation; escaped quotes; and missing or empty descriptions. |
| `src/modules/agent/AgentSkillPopup.test.ts` | assertions 7 → 15, waits 2 → 3. Proves a folded scalar contributes its continuation text instead of its indicator, literal newlines collapse to one row, and wide or astral descriptions ellipsize by display cells without truncating the skill name. |
| `src/modules/ui/BoundedListPopup.test.ts` | assertions 39 → 40, waits 13 → 13. Pins the shared desired-width authority used by popup paint and the skill-label clipping budget. |
| `src/modules/ui/WrapText.test.ts` | assertions 27 → 29, waits 15 → 15. Adds the two-cell cutoff counterexample: an overlapping wide grapheme is excluded before the ellipsis so the shared clipper cannot exceed its display-cell budget. |

- `src/modules/keybindings/KeybindingDefaults.test.ts` — assertions: 62, waits: 0
  (was 63/0) — merge reconciliation with the inline-rewrite plugin landing
  (`ac868f4`). That landing moved the inline-rewrite chords out of the canonical
  binding layer into `InlineRewriteContributor`, and deleted this file's
  `inline rewrite chords resolve without spending editor Tab` test accordingly;
  the branch had forked before that and a naive union resurrected it, where it
  asserted against a layer that no longer carries those bindings. Removed here.
  The resolution assertion it carried is preserved at
  `src/modules/inline-rewrite/InlineRewriteContributor.test.ts:113`.
  DELIBERATELY RETAINED: `inline rewrite modified chords arrive through both
  OpenTUI parsers`. That assertion (encoded bytes -> parsed event, both parser
  modes) was DROPPED by `ac868f4` with no replacement anywhere in the tree, and
  it is parser-level — independent of where the bindings are registered — so it
  still passes and still guards the #93 property that a chord must actually
  ARRIVE, not merely resolve.

| `src/modules/settings/Settings.test.ts` | assertions 72 → 71, waits 12 → 12. The removed `typescriptServer` host-field assertion became invalid when LSP took ownership of that contributed setting; `src/modules/lsp/LspPlugin.test.ts` now proves its schema and uninstall lifecycle. |
| `src/modules/keybindings/KeybindingDefaults.test.ts` | assertions 59 → 58, waits 20 → 20. The removed Ctrl+] assertion became invalid when LSP took ownership of `go.definition`; `src/modules/lsp/LspPlugin.test.ts` now proves the contributed binding and its uninstall removal. |
| `src/modules/editor/EditorCoordinates.test.ts` | assertions 42 → 0, waits 13 → 0. FILE MOVED, not removed. The shared text primitives left the source-text view (#122): this file is now `src/modules/text/TextCoordinates.test.ts` with the same 42 assertions and 13 waits, and the class it drives was renamed `EditorCoordinates` → `TextCoordinates` in the same move. No claim was dropped. |
| `src/modules/editor/TextDocument.test.ts` | assertions 59 → 0, waits 13 → 0. FILE MOVED, not removed. Now `src/modules/text/TextDocument.test.ts` with the same 59 assertions and 13 waits. No claim was dropped. |
| `src/modules/editor/TextEditing.test.ts` | assertions 16 → 0, waits 10 → 0. FILE MOVED, not removed. Now `src/modules/text/TextEditing.test.ts` with the same 16 assertions and 10 waits. No claim was dropped. |
| `src/modules/editor/TextInputModel.test.ts` | assertions 27 → 0, waits 6 → 0. FILE MOVED, not removed. Now `src/modules/text/TextInputModel.test.ts` with the same 27 assertions and 6 waits. No claim was dropped. |
| `src/modules/editor/WrapBreakOpportunity.test.ts` | assertions 17 → 0, waits 8 → 0. FILE MOVED, not removed. Now `src/modules/text/WrapBreakOpportunity.test.ts` with the same 17 assertions and 8 waits. No claim was dropped. |
| `src/modules/ui/EditorPane.test.ts` | assertions 1 → 0, waits 1 → 0. FILE MOVED, not removed. The editor's pane controller moved out of the host into its own module (#122), matching where `TerminalPaneContent` sits: now `src/modules/editor/EditorPane.test.ts` with the same 1 assertion and 1 wait. No claim was dropped. |
| `src/modules/ui/EditorPaneRenderer.test.ts` | assertions 1 → 0, waits 1 → 0. FILE MOVED, not removed. Now `src/modules/editor/EditorPaneRenderer.test.ts` with the same 1 assertion and 1 wait. No claim was dropped. |
| `src/modules/editor/Cursor.test.ts` | assertions 5 → 0, waits 1 → 0. FILE MOVED, not removed. A document cursor is a shared text primitive, not a source-text-view part (#218): now `src/modules/text/TextCursor.test.ts` with the same 5 assertions and 1 wait, and the class it drives was renamed `Cursor` → `TextCursor` in the same move. No claim was dropped. |
| `src/modules/editor/Viewport.test.ts` | assertions 4 → 0, waits 2 → 0. FILE MOVED, not removed. The scroll window one text surface shows moved with the cursor (#218): now `src/modules/text/TextViewport.test.ts` with the same 4 assertions and 2 waits, and the class was renamed `Viewport` → `TextViewport`. No claim was dropped. |
| `src/modules/structure/StructureSources.test.ts` | assertions 9 → 0, waits 2 → 0. FILE REPLACED, not weakened. `src/modules/plugins/ProviderRegistry.test.ts` carries the same reversible registration, idempotent withdrawal, last-wins fallback, revision, absent-key, and per-workspace isolation claims against the one generic host registry. Structure no longer owns a second registry to test. |
| `src/modules/ui/Breadcrumb.test.ts` | assertions 19 → 12, waits 12 → 9. The removed assertions specified history-button hit geometry for controls that no longer belong in the breadcrumb. `scripts/harness/smoke-breadcrumb-harness.ts` now proves their absence in the real frame, while `scripts/harness/smoke-navigation-history-harness.ts` preserves both navigation directions through the workspace-bar owner and keyboard bindings. |
| `scripts/harness/smoke-tabs-harness.ts` | assertions 11 → 11, waits 10 → 7. Three repeated regular-expression `.test()` calls were consolidated behind one named shared-close row predicate. The ratchet classifies those regex calls as waits by name; no observable condition wait was removed, and the same driven tab-row claim remains in the grid wait and final assertion. |
| `src/modules/ui/QuitConfirmation.test.ts` | assertions 11 → 0, waits 3 → 0. FILE REPLACED, not weakened. `src/modules/ui/Dialog.test.ts` carries the safe default, cancellation, focus movement, current confirmation action, and fresh-show reset claims against the generic dialog model now shared by quit, terminal close, and go-to-line prompt flows. |
| `scripts/harness/smoke-search-mouse-harness.ts` | assertions 11 → 11, waits 21 → 20. A pre-satisfied hover wait was superseded by one stronger pair: wait for hover PAINT to change, then read quickOpen.selectedIndex at a graph settle boundary (#478). Nothing to restore — the removed wait could not fail. |
| `scripts/harness/smoke-plugin-manifest-harness.ts` | assertions 50 → 61, waits 167 → 154. #479 (migrate the census tail) removes waits whose screen predicates were already true and replaces them with graph transitions followed by screen assertions. The shared Extensions selection walk also replaces repeated screen-change loops with one graph-index transition and one paint fence per step. No claim needs restoration because each removed wait was pre-satisfied or observed an unrelated proxy. |
| `scripts/harness/smoke-scrollbars-harness.ts` | assertions 58 → 61, waits 72 → 67. #479 (migrate the census tail) removes duplicate settled-frame, focus, and clipping waits. False-at-issue graph, status, or input-acknowledgment transitions now fence scrollbar return, editor focus, Git focus, preview leadership, and the momentum-halt click. Immediate assertions preserve the settled claims. No runtime claim was removed. |
| `scripts/harness/smoke-bracket-match-harness.ts` | assertions 6 → 6, waits 6 → 5. #480 (migrate the Quick Open idiom) replaces the pre-satisfied `sample.ts` screen wait with graph waits for the exact query and first match before Enter. The active-buffer, editor-focus, bracket-position, and cell-color claims remain unchanged. |
| `scripts/harness/smoke-git-blame-harness.ts` | assertions 7 → 7, waits 11 → 9. #480 (migrate the Quick Open idiom) replaces both pre-satisfied filename screen waits with graph waits for each exact query and first match before Enter. The tracked and untracked active-buffer, author, status-bar, and reconcile claims remain unchanged. |
| `scripts/harness/smoke-image-preview-harness.ts` | assertions 11 → 11, waits 11 → 10. #480 (migrate the Quick Open idiom) replaces the helper's pre-satisfied query-substring screen wait with graph waits for the exact query and first match before Enter. All four call sites still assert the opened path and its text, binary, PNG, or JPEG projection. |
| `scripts/harness/Drive.ts` | assertions 0 → 0, built-in gesture waits 6 → 0. The user vetoed the five-name `--gesture` command-line layer and its five panel roles. Primitive `DriveSession` chains supersede that app-specific vocabulary. The primitive `--key`, `--click`, `--hover`, `--wait-for-text`, and `--wait-for-status` actions remain, and the generic `status-excludes` completion remains available. |
| `scripts/tui-harness.sh` | assertions 0 → 0, fixed delay sites 12 → 11. The focus delay became the named changed-screen condition used by `settle`; focus-out is declared frame-silent and focus-in waits for a changed completed screen. The other 11 delays remain because they include gesture travel or pacing and serve consumers that can be frame-silent or clamped. The shared `settle` check is stronger: it now requires a screen or caret change from before the driven input, fenced by a later completed quiescent publication. |
